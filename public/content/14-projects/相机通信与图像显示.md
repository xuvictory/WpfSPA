---
title: 相机通信与图像显示
section: 14-projects
parent: 14.5 项目五：工业视觉检测上位机（中高级）
---

# 相机通信与图像显示

> [!plain] 白话理解
> "相机通信与图像显示"是 WPF 上位机开发中的一项重要知识。在学习 WPF 上位机开发的过程中，"相机通信与图像显示"是一个重要的知识点。把前面学的一切串起来！通过完整的工业级项目，体验真实的上位机开发全流程。掌握了它，你就能更好地构建工业级上位机应用程序。

> [!def] 官方定义
> 相机通信与图像显示是 WPF / .NET 技术栈中由微软官方定义和实现的一个特性/概念/控件。它遵循 .NET 标准规范，为开发者提供了一套完整的编程接口（API）和最佳实践指南。详细定义请参考 Microsoft Docs 官方文档。

> [!origin] 由来背景
> 相机通信与图像显示的诞生源于实际开发中的痛点。微软在设计 .NET 和 WPF 框架时，为了满足企业级应用（尤其是工业自动化、数据可视化等场景）的需求，引入了这一特性。它的设计理念参考了业界最佳实践，并在日后的版本迭代中不断优化。

> 本章节背景：把前面学的一切串起来！通过完整的工业级项目，体验真实的上位机开发全流程。

> [!essentials] 核心要点
> - **概念理解**：首先搞清楚"相机通信与图像显示"是什么，它解决了什么问题
> - **关键 API**：掌握最常用的属性和方法，能用代码表达你的意图
> - **使用模式**：了解惯用的写法套路，避免重复造轮子
> - **注意事项**：知道什么能做，什么不能做，踩坑前先看清路
> - **实战检验**：用一个小项目或练习来验证你真的理解了

> [!example] 完整示例
> **相机通信与图像显示演示：模拟工业相机以 10FPS 连续采集图像（用 WriteableBitmap 生成灰度噪声帧模拟实时图像流），右侧显示相机参数与实时帧率，点击"拍照"可将当前帧保存为 PNG：**
>
> **MainWindow.xaml：**
> ```xml
> <Window x:Class="HmiDemo.MainWindow"
>         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
>         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
>         Title="相机通信与图像显示" Height="480" Width="720"
>         WindowStartupLocation="CenterScreen" Background="#0D1117">
>     <Grid Margin="15">
>         <Grid.ColumnDefinitions>
>             <ColumnDefinition Width="*"/>
>             <ColumnDefinition Width="12"/>
>             <ColumnDefinition Width="180"/>
>         </Grid.ColumnDefinitions>
>         <Grid.RowDefinitions>
>             <RowDefinition Height="Auto"/>
>             <RowDefinition Height="*"/>
>             <RowDefinition Height="Auto"/>
>         </Grid.RowDefinitions>
>         <TextBlock Grid.ColumnSpan="3" Text="工业相机采集与图像显示" Foreground="#58A6FF"
>                    FontSize="14" FontWeight="Bold" Margin="0,0,0,8"/>
>         <!-- 图像显示区 -->
>         <Border Grid.Row="1" Grid.Column="0" Background="#161B22" CornerRadius="6">
>             <Image x:Name="CamImage" Stretch="Uniform" Margin="6"/>
>         </Border>
>         <!-- 相机参数区 -->
>         <Border Grid.Row="1" Grid.Column="2" Background="#161B22" CornerRadius="6" Padding="10">
>             <StackPanel>
>                 <TextBlock Text="相机状态" Foreground="#58A6FF" FontWeight="Bold" Margin="0,0,0,8"/>
>                 <TextBlock Text="型号" Foreground="#8B949E"/>
>                 <TextBlock Text="MV-CE060-10GM" Foreground="#8B949E" Margin="0,2,0,8"/>
>                 <TextBlock Text="分辨率" Foreground="#8B949E"/>
>                 <TextBlock Text="640 × 480" Foreground="#8B949E" Margin="0,2,0,8"/>
>                 <TextBlock Text="当前帧率" Foreground="#8B949E"/>
>                 <TextBlock x:Name="FpsText" Text="-- FPS" Foreground="#58A6FF" FontSize="20"
>                            FontWeight="Bold" Margin="0,2,0,8"/>
>                 <TextBlock Text="采集帧数" Foreground="#8B949E"/>
>                 <TextBlock x:Name="FrameText" Text="0" Foreground="#8B949E" Margin="0,2,0,0"/>
>             </StackPanel>
>         </Border>
>         <StackPanel Grid.Row="2" Grid.ColumnSpan="3" Orientation="Horizontal" Margin="0,10,0,0">
>             <Button x:Name="CamBtn" Content="开始采集" Click="OnToggle" Padding="10"
>                     Background="#21262D" Foreground="White"/>
>             <Button Content="拍照（保存当前帧）" Click="OnSnapshot" Padding="10" Margin="8,0,0,0"
>                     Background="#21262D" Foreground="White"/>
>             <TextBlock x:Name="StatusText" Text="相机离线" Foreground="#8B949E"
>                        VerticalAlignment="Center" Margin="12,0,0,0"/>
>         </StackPanel>
>     </Grid>
> </Window>
> ```
>
> **MainWindow.xaml.cs —— 后台代码：**
> ```csharp
> using System;
> using System.IO;
> using System.Runtime.InteropServices;
> using System.Windows;
> using System.Windows.Media;
> using System.Windows.Media.Imaging;
> using System.Windows.Threading;
>
> namespace HmiDemo
> {
>     public partial class MainWindow : Window
>     {
>         private const int FrameW = 640, FrameH = 480;
>         private readonly DispatcherTimer _timer = new DispatcherTimer();
>         private readonly Random _rand = new Random();
>         private WriteableBitmap _bitmap;
>         private int _frames, _fpsCount;
>         private DateTime _last = DateTime.Now;
>
>         public MainWindow()
>         {
>             InitializeComponent();
>             _timer.Interval = TimeSpan.FromMilliseconds(100); // 模拟 10FPS 采集
>             _timer.Tick += OnCapture;
>         }
>
>         private void OnToggle(object sender, RoutedEventArgs e)
>         {
>             if (_timer.IsEnabled)
>             {
>                 _timer.Stop(); CamBtn.Content = "开始采集"; StatusText.Text = "相机停止";
>             }
>             else
>             {
>                 _timer.Start(); CamBtn.Content = "停止采集";
>                 StatusText.Text = "采集中…"; StatusText.Foreground = Brushes.LimeGreen;
>             }
>         }
>
>         // 模拟相机采集一帧：生成灰度噪声图（真实项目为相机 SDK 的图像回调）
>         private void OnCapture(object sender, EventArgs e)
>         {
>             if (_bitmap == null)
>             {
>                 _bitmap = new WriteableBitmap(FrameW, FrameH, 96, 96, PixelFormats.Gray8, null);
>                 CamImage.Source = _bitmap;
>             }
>             var buffer = new byte[FrameW * FrameH];
>             _rand.NextBytes(buffer);
>             _bitmap.Lock();                                    // 锁定后台缓冲区
>             Marshal.Copy(buffer, 0, _bitmap.BackBuffer, buffer.Length);
>             _bitmap.AddDirtyRect(new Int32Rect(0, 0, FrameW, FrameH));
>             _bitmap.Unlock();                                  // 解锁并刷新显示
>
>             // 帧率统计：每秒结算一次
>             _frames++; _fpsCount++;
>             var now = DateTime.Now;
>             if ((now - _last).TotalSeconds >= 1)
>             {
>                 FpsText.Text = $"{_fpsCount} FPS";
>                 _fpsCount = 0; _last = now;
>             }
>             FrameText.Text = _frames.ToString();
>         }
>
>         // 拍照：把当前帧编码为 PNG 保存到程序目录
>         private void OnSnapshot(object sender, RoutedEventArgs e)
>         {
>             if (_bitmap == null) return;
>             var encoder = new PngBitmapEncoder();
>             encoder.Frames.Add(BitmapFrame.Create(_bitmap));
>             encoder.Save(File.Create("snapshot.png"));
>             StatusText.Text = $"已保存 snapshot.png（第 {_frames} 帧）";
>         }
>     }
> }
> ```
> 
> 

> [!scene] 适用场景
> ✅ 上位机数据展示与交互界面开发
> ✅ 工业自动化设备状态监控系统
> ✅ 需要高效数据绑定的实时数据处理场景
> ✅ 多窗口、多页面复杂导航的企业级应用
> ❌ 简单的控制台工具程序（用控制台更省事）
> ❌ 对性能要求极端苛刻的底层驱动开发（用 C++ 更合适）

> [!pitfall] 常见踩坑
> 坑 1：**概念理解不清就上手** → 建议先把本章节的前置知识点学完，理解基础原理后再动手写代码
> 
> 坑 2：**忽略了官方文档** → Microsoft Docs 上有最权威的说明和最完整的示例代码，遇到问题先查文档
>
> 坑 3：**代码写的太"一次性"** → 养成写可复用代码的习惯，以后项目中会反复用到这些知识

> [!best] 最佳实践
> - 编写代码时保持一致的命名规范（PascalCase 用于公共成员，_camelCase 用于私有字段）
> - 善用 Visual Studio 的智能提示和代码片段，提高开发效率
> - 每个关键代码块加上注释，解释"为什么这样写"而不仅仅是"写的是什么"
> - 遵循 SOLID 原则，尤其是单一职责原则：一个类只做一件事
> - 经常重构：写完功能后回头看看有没有更简洁的写法

> [!practice] 上手练习
> **Lv.1 照猫画虎**：阅读并运行本节示例代码，确保程序可以正常运行，修改一些参数观察效果变化
> **Lv.2 小试牛刀**：在示例代码的基础上，添加一个小功能或修改一项设置，观察程序的响应
> **Lv.3 融会贯通**：结合前面学过的知识，用"相机通信与图像显示"实现一个上位机中的小功能模块

> [!related] 相关知识链接
> - ← 前置知识：请确保你已经理解了本章节之前的内容，再学习"相机通信与图像显示"
> - → 后续必学：掌握"相机通信与图像显示"后，建议接着学习本章节的下一个知识点
> - ⇄ 关联概念：数据绑定、命令系统、MVVM 模式（WPF 开发的核心支柱）
> - 📖 官方文档：https://learn.microsoft.com/zh-cn/dotnet/desktop/wpf/
