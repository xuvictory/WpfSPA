---
name: 完成第7章剩余11篇高质量重写
overview: 按归档文档第 8 节"高质量文章写作规范"，重写 public/content/07-mvvm/ 下剩余 11 篇文章的 9 个 Callout 段落（plain/def/origin/essentials/scene/pitfall/best/practice/related），保留已完成的 example，删除游离段落，校验后提交。
todos:
  - id: rewrite-di-group
    content: 按第8节规范重写7.6依赖注入4篇（什么是依赖注入、容器组成与生命周期、常用-di-容器、di-在-mvvm-中的应用），保留示例、删除游离段落
    status: completed
  - id: rewrite-practice-group
    content: 按第8节规范重写7.7实战5篇（项目结构与目录规划、配置服务、日志服务集成、对话框服务、导航服务实现），保留示例、删除游离段落
    status: completed
  - id: rewrite-framework-group
    content: 按第8节规范重写7.5框架2篇（prism-企业级框架、reactiveui-响应式框架），保留示例、删除游离段落
    status: completed
  - id: validate
    content: 校验07-mvvm废话特征计数清零、无+前缀残留、抽查2-3篇示例XAML/C#对应关系、git diff --stat确认改动范围
    status: completed
    dependencies:
      - rewrite-di-group
      - rewrite-practice-group
      - rewrite-framework-group
  - id: commit
    content: 用UTF-8消息文件提交07-mvvm改动（提交信息"7.MVVM设计模式菜单内容更新"），提交后删除消息文件
    status: completed
    dependencies:
      - validate
---

## 用户需求

继续第 7 章「MVVM 设计模式」菜单内容的高质量更新。此前已完成 17/28 篇，本次按 `章节内容更新经验归档.md` 第 8 节「高质量文章写作规范」重写剩余 11 篇仍残留"废话模板"的文章，使全章达到统一高质量标准。

## 产品概述

这是纯内容（Markdown）重写任务，不改动 `meta.json`、`quizzes.json`、`src/`、`styles/` 及任何代码。每篇文章保留标题、frontmatter 与已填充的 `[!example] 完整示例`（含 XAML + C#），仅将其余 9 个 Callout 卡片——白话理解、官方定义、由来背景、核心要点、适用场景、常见踩坑、最佳实践、上手练习、相关知识链接——逐条替换为贴合该主题、贴合上位机/工控场景的真实内容，并删除「本章节背景」等游离段落。视觉呈现保持平台既有暗色工控科技风与十段式 Callout 卡片体系，不变更任何 UI。

## 核心功能

- 按第 8 节规范逐篇重写以下 11 篇文章：
- 7.6 依赖注入（4 篇）：`什么是依赖注入`、`容器组成与生命周期`、`常用-di-容器`、`di-在-mvvm-中的应用`
- 7.7 MVVM 实战（5 篇）：`项目结构与目录规划`、`配置服务`、`日志服务集成`、`对话框服务`、`导航服务实现`
- 7.5 主流框架（2 篇）：`prism-企业级框架`、`reactiveui-响应式框架`
- 各 Callout 替换要求：
- `[!plain]`：工控场景生活化类比讲清本质，不复读标题
- `[!def]`：真实准确的定义，区分架构模式/框架/接口/控件，关键 API 写全限定归属，附官方文档链接
- `[!origin]`：真实历史与动机（如 DI 源于依赖倒置原则、Prism 为微软模式与实践团队开源）
- `[!essentials]`：该主题真正相关的要点清单
- `[!scene]`：各篇不得雷同的具体适用/不适用场景
- `[!pitfall]`：含"现象 → 原因 → 解决/预防"的真实踩坑
- `[!best]`：可落地的主题相关实践
- `[!practice]`：与主题挂钩的 Lv.1-Lv.4 具体练习
- `[!related]`：真实站内互链（07 章内用文件名）+ learn.microsoft.com 具体页链接
- 完成后执行批量校验（废话特征计数清零、无 `+` 前缀残留、抽查示例 XAML/C# 对应关系），并以 UTF-8 消息文件提交。

## 技术说明

本任务为 Markdown 内容重写，不涉及项目技术栈改动。修改目标仅限 `public/content/07-mvvm/` 下 11 个 `.md` 文件，沿用既有文件结构与 Callout 语法（`> [!类型] 标题` + 引用行）。执行时遵循归档文档的纪律：同文件串行编辑、匹配串精确（区分真实空行与 `>` 引用空行）、每篇逐条替换。示例部分（`[!example]`）保持原样，仅复核 XAML 中 `x:Name`/`Click`/`Command` 与 C# 的对应关系。提交采用 UTF-8 消息文件方式（`git commit -F _msg.txt`），避免 PowerShell 下中文提交信息被存为 GBK 乱码。