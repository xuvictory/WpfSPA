---
name: 完成第14章工业级实战项目高质量重写
overview: 按《章节内容更新经验归档.md》第 8 节"高质量文章写作规范"，重写第 14 章（14-projects）26 篇文章中除完整示例外的 9 个 Callout（白话理解/官方定义/由来背景/核心要点/适用场景/常见踩坑/最佳实践/上手练习/相关知识链接），删除游离的"本章节背景"段落，校验后提交。
todos:
  - id: scan-ch14
    content: 使用 [subagent:code-explorer] 扫描14章26篇现状并生成重写规划表
    status: completed
  - id: rewrite-141
    content: 重写14.1温湿度监控6篇的9个Callout并删除游离段落
    status: completed
    dependencies:
      - scan-ch14
  - id: rewrite-142
    content: 重写14.2 Modbus看板3篇的9个Callout并删除游离段落
    status: completed
    dependencies:
      - scan-ch14
  - id: rewrite-143
    content: 重写14.3产线监控4篇的9个Callout并删除游离段落
    status: completed
    dependencies:
      - scan-ch14
  - id: rewrite-144
    content: 重写14.4 WMS仓储4篇的9个Callout并删除游离段落
    status: completed
    dependencies:
      - scan-ch14
  - id: rewrite-145
    content: 重写14.5视觉检测2篇的9个Callout并删除游离段落
    status: completed
    dependencies:
      - scan-ch14
  - id: rewrite-146
    content: 重写14.6运动控制2篇的9个Callout并删除游离段落
    status: completed
    dependencies:
      - scan-ch14
  - id: rewrite-147
    content: 重写14.7 SCADA监控5篇的9个Callout并删除游离段落
    status: completed
    dependencies:
      - scan-ch14
  - id: verify-commit
    content: 校验模板词条清零、更新经验归档并UTF-8提交14章更新
    status: completed
    dependencies:
      - rewrite-141
      - rewrite-142
      - rewrite-143
      - rewrite-144
      - rewrite-145
      - rewrite-146
      - rewrite-147
---

## 用户需求

优化第 14 章「工业级实战项目」：对 `public/content/14-projects/` 下全部 26 篇文章进行高质量重写输出，与第 07/08/10/11/12 章保持同一质量标准。

## 现状确认

- 26 篇文章的 `[!example] 完整示例` 已全部填充（代码规范达标：HmiDemo 命名空间、暗色工控主题、XAML 与 C# 对应），**示例保留为主**，仅对明显问题微调。
- 其余 9 个 Callout（白话理解/官方定义/由来背景/核心要点/适用场景/常见踩坑/最佳实践/上手练习/相关知识链接）全部为全站通用"废话模板"，逐篇存在雷同套话，需按《章节内容更新经验归档.md》第 8 节规范重写。
- 每篇存在游离段落"本章节背景：把前面学的一切串起来！…"需删除。
- 不修改 meta.json、quizzes.json、src/、styles/ 等其余内容。

## 重写要求（按第 8 节规范）

- 每条 Callout 必须贴合该篇主题与所属子项目（温湿度监控 / Modbus 看板 / 产线监控 OEE / WMS / 视觉检测 / 运动控制 / SCADA），禁止套话，各篇不得雷同。
- `[!related]` 使用真实站内互链（14 章内用文件名，跨章用「第 N 章·文章名」）与 learn.microsoft.com 具体页。
- 删除 `[!origin]` 后残留的游离段落。
- 完成后校验模板词条清零并提交。

## 交付物

- 26 篇高质量重写的 Markdown 文章
- 校验通过（模板词条计数为 0）
- 一次 git 提交（UTF-8 提交信息）

## 技术方案

### 实施策略

- 本任务为纯内容重写，不涉及代码库功能变更；沿用既有文章结构与 Callout 渲染机制，仅编辑 Markdown 正文。
- 每篇文章重写 `[!plain]`、`[!def]`、`[!origin]`、`[!essentials]`、`[!scene]`、`[!pitfall]`、`[!best]`、`[!practice]`、`[!related]` 九个 Callout，删除游离段落；`[!example]` 示例代码保留（规范已达标），仅当发现 XAML/C# 明显不对应时修正。
- 写作参照标杆：`public/content/07-mvvm/什么是-mvvm.md`；代码模板参照 `public/content/04-controls/button-按钮.md`。
- 素材要求真实：Modbus（1979 年 Modicon 提出）、OEE（源自 TPM）、RFID、GigE Vision、运动控制卡等工业背景须真实准确。

### 执行纪律（沿用经验归档第 8.4 节）

- 同一文件必须串行编辑，改完一个读一个再改下一个，禁止并行写同一文件。
- 编辑时精确匹配，区分真正的空行与 `>` 引用空行。
- 每批完成后立即用 search_content 检查 `^\+` 前缀污染。

### 校验标准

- search_content 在 `14-projects` 目录确认以下词条计数为 0：`是 WPF 上位机开发中的一项重要知识`、`微软官方定义和实现`、`Lv.1 照猫画虎`、`请确保你已经理解了本章节之前的内容`、`本章节背景`。
- git diff --stat 确认改动仅涉及 `public/content/14-projects/`。

### 提交纪律（经验归档坑 #5）

- 提交信息「14.工业级实战项目菜单内容更新」须通过 UTF-8 消息文件提交（先写 `_msg.txt`，再 `git commit -F _msg.txt`，提交后删除 `_msg.txt`），禁止 `git commit -m "中文"`（会存成 GBK）。
- 只 add `public/content/14-projects/` 与归档文件；提交后更新《章节内容更新经验归档.md》状态总览表（14-projects 标记为已完成）。

## Agent Extensions

### SubAgent

- **code-explorer**
- 用途：批 1 启动时批量扫描 14 章 26 篇文章，提取每篇的标题、模板残留 Callout、示例主题方向与代码要点，形成逐篇重写规划表（含站内互链文件名清单），供后续 7 个批次直接引用。
- 预期产出：每篇文章的模板特征清单、示例代码知识点摘要、14 章内可互链的文章清单，确保重写内容真实准确、互链有效。