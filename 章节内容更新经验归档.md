# 章节内容更新经验归档（AI 交接文档）

> 本文档用于归档"完成菜单第 N 章代办事项"这类任务的完整经验，供后续 AI 处理 07-16 章时直接复用，无需重新熟悉项目。
>
> **一句话总结任务本质**：把 `public/content/{NN-章节}/` 下全部文章的 `> [!example] 完整示例` 占位符（`// 📝 待补充实际示例代码`）替换为**真实可运行、贴合上位机/工控场景**的 WPF 示例（XAML + C#），逐篇编写，完成后校验并提交一次 commit。
>
> 最后更新：2026-08-17（第 6 章完成后）

---

## 1. 项目结构速览

- **项目性质**：WPF 上位机开发在线学习平台，纯前端 SPA（Vite 6 + 原生 JS + marked 15 + highlight.js），无后端。
- **入口**：`index.html`；源码 `src/`（7 个 JS 模块：main/router/markdown/ui/search/progress/quiz）；样式 `styles/`（4 个 CSS）。
- **内容目录**：`public/content/`，共 17 个章节目录（00-16）+ `meta.json`（菜单树）+ `quizzes.json`（测验题）。
- **每篇文章结构**："十段式深度教学法"，10 种 Callout 卡片：`[!plain]白话理解`、`[!def]官方定义`、`[!origin]由来背景`、`[!essentials]核心要点`、`[!example]完整示例`、`[!scene]适用场景`、`[!pitfall]常见踩坑`、`[!best]最佳实践`、`[!practice]上手练习`、`[!related]相关知识链接`。
- **路由**：Hash 路由 `#/chapter/{章节}/{slug}`；slug 由 `meta.json` 中 `path` 定义。
- **开发/构建**：`npm run dev`（端口 5174）、`npm run build`。
- 需求文档见 `需求说明文档.md`，菜单大纲见 `完整知识菜单.md`。

---

## 2. 完成状态总览

| 章节 | 目录 | 文章数 | 状态 | 对应提交 |
| --- | --- | --- | --- | --- |
| 0. 编程先导课 | `00-prelude` | 94 | ✅ 已完成 | d3da93f（初始化自带） |
| 1. WPF 快速入门 | `01-quickstart` | 21 | ✅ 已完成 | c7a5662 |
| 2. XAML 详解 | `02-xaml` | 10 | ✅ 已完成 | 517b34e |
| 3. 布局系统 | `03-layout` | 20 | ✅ 已完成 | b28095d |
| 4. 控件体系 | `04-controls` | 48 | ✅ 已完成（**标准模板发源地**） | bfa0448 |
| 5. WPF 核心概念 | `05-core-concepts` | 58 | ✅ 已完成 | b28095d |
| 6. 图形与多媒体 | `06-graphics` | 43 | ✅ 已完成（按标准模板） | f4944f2 |
| **7. MVVM 设计模式** | **`07-mvvm`** | **28** | ⏳ **待处理（下一个）** | — |
| 8. 线程与异步 | `08-threading` | 21 | ⏳ 待处理 | — |
| 9. 上位机通信协议 | `09-communication` | 33 | ⏳ 待处理 | — |
| 10. 数据采集与处理 | `10-data-collection` | 21 | ⏳ 待处理 | — |
| 11. WPF 高级 UI 开发 | `11-advanced-ui` | 31 | ⏳ 待处理 | — |
| 12. 系统架构与设计模式 | `12-architecture` | 29 | ⏳ 待处理 | — |
| 13. 性能优化与调试 | `13-performance` | 23 | ⏳ 待处理 | — |
| 14. 工业级实战项目 | `14-projects` | 26 | ⏳ 待处理 | — |
| 15. 部署与发布 | `15-deployment` | 10 | ⏳ 待处理 | — |
| 16. 开源项目与学习资源 | `16-resources` | 33 | ⏳ 待处理 | — |

- 待处理章节合计 **255 篇**（与占位符检索计数 255 完全吻合，可作校验依据）。
- **注意**：00-03、05 完成时间较早，模板相对简单；**以 04（bfa0448）和 06（f4944f2）为最新标准模板参照**。

---

## 3. 更新模式与代码模板（核心）

### 3.1 只改什么、不改什么

- **只改**：`[!example] 完整示例` 卡片内部的内容。其余 9 个 Callout 段落一律保持原样（包括其中的占位式通用文案，不要去润色）。
- **不改**：`meta.json`、`quizzes.json`、`src/`、`styles/`、`index.html`、其他章节目录。
- 每篇文件仅动 `[!example]` 一处，**一个文件只做一次替换**。

### 3.2 替换后的标准结构（严格照抄）

替换占位符为：

```
> **{上位机场景描述}：{本节知识点覆盖的要点}：**
>
> **MainWindow.xaml：**
> ```xml
> <Window x:Class="HmiDemo.MainWindow" ...>
>     ...（UI，x:Name 与 C# 一一对应）
> </Window>
> ```
>
> **MainWindow.xaml.cs —— 后台代码：**
> ```csharp
> using ...;
> namespace HmiDemo
> {
>     public partial class MainWindow : Window
>     {
>         ...
>     }
> }
> ```
>
```

### 3.3 代码编写规范（06 章确立的细则）

- 命名空间统一 `HmiDemo`，窗口类 `MainWindow`，`x:Class="HmiDemo.MainWindow"`。
- 窗口固定暗色工控主题：
  - 窗口背景 `Background="#0D1117"`；
  - 面板/容器 `#161B22`、按钮灰 `#21262D`；
  - 强调蓝 `#58A6FF`（标题）、正文灰 `#8B949E`；
  - 成功绿 `#238636`、危险红 `#DA3633`。
- 中文注释说明关键 API 用途；示例必须**展示本篇文章独有知识点**（例如 Line 用 X1/Y1/X2/Y2、Path 用 Data 微语言、WriteableBitmap 用 WritePixels、DispatcherTimer 定时刷新等），避免"万能模板"。
- 场景必须贴合上位机/工控：设备启停、参数下发、状态灯、仪表指针、实时曲线、报警闪烁、监控看板、报警音、数据采集、趋势图、PLC 点表等。
- XAML 的 `Click` 事件、`x:Name`、`Loaded` 等与 C# 后台代码**严格一一对应**，保证可编译运行。
- 代码块语言标记：XAML 用 `xml`，后台代码用 `csharp`。
- 概念性文章（如性能注意事项、应用场景、概述类）也**必须给出可运行的最小演示窗口**，不能只写说明文字。
- 涉及异步/线程（08 章起）时注意：UI 更新须 `Dispatcher.Invoke`，示例代码仍要保持可运行、可理解。
- 涉及文件 IO/路径（如保存 PNG、播放媒体）时，路径用相对路径/内置资源，避免依赖用户机器的绝对路径。

### 3.4 示例主题规划方法

按 meta.json 中的 subgroup 分批规划，每篇给一个贴合本知识点且不重复的上位机主题。以 06 章为例的规划表（可复用于后续章节）：

| 子章节 | 篇数 | 示例主题方向 |
| --- | --- | --- |
| 6.1 渲染概述 | 1 | 设备看板（保留模式渲染） |
| 6.2 Shape | 7 | Line 温度曲线、Rectangle 工位栅格、Ellipse 转速表、Polygon 危险区、Polyline PLC 波形、Path 齿轮图标、Shape 共享属性 |
| 6.3 Path/Geometry | 3 | GeometryGroup 罐体液位、路径微语言压力表、Path 指示灯面板 |
| 6.4 Brush | 7 | 状态灯、料位条、球形指示灯、产品图墙、仪表倒影、背景纹路、综合看板 |
| 6.5 Transform | 7 | 风机旋转、画面缩放、倾斜标牌、传送带、机械臂组合、矩阵变换、Render vs Layout 对比 |
| 6.6 Effect | 3 | 失焦模糊、悬浮卡片投影、8 路模糊性能对比 |
| 6.7 图像处理 | 4 | Image 控件、BitmapImage 解码、WriteableBitmap 示波器、RenderTargetBitmap 快照 |
| 6.8 音视频 | 3 | MediaElement 培训视频、SoundPlayer 报警音、音频综合（长语音+短提示） |
| 6.9 2D/3D | 2 | OnRender 自绘输送线、Viewport3D 设备箱体 |
| 6.10 动画 | 6 | 心跳灯、三类基础动画、机械臂抓取关键帧、缓动对比、开机自检 Storyboard、综合看板 |

**07-16 章主题方向提示**（后续可细化）：
- 07 MVVM：ViewModel 状态机、命令绑定面板、INPC 实时数据、依赖注入容器、导航示例；
- 08 线程：生产者-消费者、后台采集+Dispatcher 刷新、任务并行采集、并发集合模拟；
- 09 通信：串口调试面板、TCP 客户端、Modbus 读寄存器、MQTT 订阅、WebSocket 看板；
- 10 数据采集：采集+曲线联动、数据校验过滤、SQLite 存取、报警列表；
- 11 高级 UI：自定义控件、主题切换、拖拽、DPI 适配；
- 12 架构：插件加载、单例配置、观察者日志、自动更新；
- 13 性能：虚拟化列表、内存分析演示、渲染层级；
- 14 实战：温湿度监控、PLC 看板、OEE、WMS 等（整章可做成一个主工程多窗口）；
- 15 部署：发布配置、开机自启、版本检测；
- 16 资源：资源文章可为"工具介绍 + 最小使用示例"（如 NModbus 读寄存器、LiveCharts 画曲线、CommunityToolkit.Mvvm 一个 ViewModel）。

---

## 4. 标准执行流程 SOP（处理第 7 章时照做）

1. **读清单**：从 `public/content/meta.json` 用 `search_content`（文件太大，约 4200 行，**禁止 read_file 全文**）按章节 id（如 `07-mvvm`）提取全部 subgroup 和 leaf 的 `file` 路径，形成待处理文件清单。
2. **定参照**：读 1-2 篇上一章已完成的文章（推荐 `04-controls/button-按钮.md`、`06-graphics/wpf-图形渲染概述.md`、`06-graphics/writeablebitmap-可写入位图.md`）确认模板细节。
3. **按 subgroup 分批编写**：建议 5-7 篇为一批，批内用 `todo_write` 跟踪（每批一个 todo）。逐篇 `replace_in_file` 替换占位符。
   - 统一旧串：`> [!example] 完整示例\n> ```csharp\n> // 📝 待补充实际示例代码\n> // 请根据本节知识点编写一个能运行的 Demo\n> ```\n> `（每篇完全一致，可直接复用）。
4. **注意换行细节**：替换后 `[!example]` 卡片结尾通常留一行 `> `（多余引用空行），与全章风格保持一致即可，不用清理成完全干净。
5. **校验**（见第 5 节）。
6. **提交**：`git add public/content/{NN-章节}` 后 `git commit -m "{N}.{章节名}菜单内容更新"`（参照历史命名，如 `"7.MVVM设计模式菜单内容更新"`）。

---

## 5. 校验方法（每次完成后必须执行）

```bash
# 1) 确认该章节占位符全部清零（全库搜索 + 按章节确认无残留）
#    search_content pattern=待补充实际示例代码 outputMode=count → 结果应只剩未处理章节
# 2) 确认改动只涉及目标章节
git diff --stat
# 3) 确认没有 + 前缀残留等格式污染
#    search_content pattern=^\+|^\+>  检查目标目录
# 4) 建议抽查 2-3 篇渲染后的 XAML/C# 对应关系（x:Name、Click 事件）
```

预期结果（以 06 章为例）：43 files, +3110/-86。

---

## 6. 常见坑（血泪教训）

1. **PowerShell 中文乱码**：终端查看含中文文件的 git diff 会乱码；规避方法：`chcp 65001` 设置 UTF-8 代码页、用 `git --no-pager`、用通配符路径、避免在命令中直接输入中文路径。`| cat` 在 PowerShell 不可用（cat 是 Get-Content 别名），不要管道。
2. **替换内容混入 `+` 前缀**：曾有两次把 `+ ` 前缀混进正文（3d-图形入门.md、基础动画类型.md），用 `search_content pattern=^\+` 检出后整体重写修复。**写完每批立刻搜索 `^\+` 检查**。
3. **布局陷阱**：缓动函数.md 原用 `Canvas.Left` 动画但元素在 StackPanel 内不生效 → 改用 `TranslateTransform` 承载。写动画/定位类示例时先确认元素所在容器。
4. **meta.json 太大**：全文 13.8 万字符，必须用 `search_content` 局部提取，禁止 `read_file` 全文。
5. **提交信息乱码不影响实际**：`git commit -m "中文"` 在终端显示乱码，但仓库内属性实际正确（可用 `git log --format=%s` 确认）。
6. **git log 某目录**：`git --no-pager log --oneline -- <目录>` 可查该目录的提交历史（注意 PowerShell 下路径不加引号、不接管道）。
7. **每个文件只改一处**：`replace_in_file` 的 old_str 是全章统一占位符串，各文件唯一，放心替换；若某文件占位符格式不同，先 read_file 看实际内容再改。

---

## 7. 相关文件速查

| 用途 | 路径 |
| --- | --- |
| 菜单树（章节/子分组/文章清单） | `public/content/meta.json` |
| 测验题库 | `public/content/quizzes.json` |
| 需求说明 | `需求说明文档.md` |
| 课程大纲 | `完整知识菜单.md` |
| 标准模板参照 | `public/content/04-controls/button-按钮.md` |
| 概念性文章示例参照 | `public/content/06-graphics/wpf-图形渲染概述.md` |
| 历史计划存档 | `.codebuddy/plans/`（如 `完成第6章图形与多媒体文章示例代码_e44ef550.md`） |
