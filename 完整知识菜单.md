# WPF上位机开发学习平台 —— 完整知识菜单

---

## 0. 编程先导课

### 0.1 C# 基础速览
- 开发环境搭建
  - Visual Studio 2022 安装与配置
  - .NET SDK 安装
  - 第一个控制台程序：Hello World
- 变量与数据类型
  - 值类型（int、float、double、bool、char、decimal、byte）
  - 引用类型（string、object）
  - 类型转换（隐式转换、显式转换、Convert、Parse、TryParse）
  - var 类型推断
  - 可空类型（Nullable<T> / T?）
  - 常量和只读字段（const vs readonly）
- 运算符与表达式
  - 算术运算符（+、-、*、/、%）
  - 比较运算符（==、!=、>、<、>=、<=）
  - 逻辑运算符（&&、||、!）
  - 赋值运算符（=、+=、-=、*=、/=）
  - 三元运算符（?:）
  - 空合并运算符（??、??=）
  - 位运算符（&、|、^、~、<<、>>）
- 流程控制
  - 条件语句（if-else、else if）
  - switch 语句与 switch 表达式
  - for 循环
  - while 循环与 do-while 循环
  - foreach 循环
  - break、continue、return
- 字符串操作
  - 字符串拼接与格式化
  - 字符串插值（$""）
  - 常用方法（Substring、IndexOf、Replace、Split、Join、Trim、Contains、StartsWith、EndsWith）
  - StringBuilder
- 数组与集合
  - 一维数组与多维数组
  - List\<T\> 泛型集合
  - Dictionary\<TKey, TValue\> 字典
  - HashSet\<T\> 集合
  - Queue\<T\> 与 Stack\<T\>
  - ObservableCollection\<T\>（上位机必用）
  - LINQ 基础（Where、Select、OrderBy、First、Any、Count、ToList）
- 类与对象
  - 类的定义与实例化
  - 字段与属性（get/set、自动属性）
  - 构造函数（无参、有参、构造重载）
  - 方法（参数传递：值传递、ref、out）
  - 方法重载
  - 静态成员（static field、static method、static class）
  - 命名空间（namespace）与 using
- 封装与访问修饰符
  - public、private、protected、internal
  - 属性封装字段的技巧
- 继承与多态
  - 基类与派生类
  - virtual、override、new 关键字
  - abstract 抽象类与抽象方法
  - sealed 密封类
  - 接口（interface）定义与实现
  - 接口 vs 抽象类的选择
- 异常处理
  - try-catch-finally
  - throw 抛出异常
  - 自定义异常类
  - using 语句与 IDisposable
- 委托与事件
  - delegate 委托定义
  - Action\<T\> 与 Func\<T\>
  - event 事件声明与触发
  - EventHandler 与 EventArgs
  - 匿名方法与 Lambda 表达式
- 泛型
  - 泛型类
  - 泛型方法
  - 泛型约束（where T : class / new() / 接口）
- 异步编程（上位机核心技能）
  - 同步 vs 异步概念
  - Task 与 Task\<T\>
  - async / await 关键字
  - ConfigureAwait(false) 在UI线程中的重要性
  - Task.Run 与线程池
  - Task.Delay 与计时
  - CancellationToken 取消令牌
  - 并行任务（Task.WhenAll、Task.WhenAny）
- 文件与 IO 操作
  - File 类（ReadAllText、WriteAllText、AppendAllText、Exists、Delete）
  - FileStream 读写文件流
  - StreamReader / StreamWriter
  - Path 类（路径合并、获取扩展名、获取文件名）
  - Directory 类（创建目录、遍历文件）
- JSON 序列化与反序列化
  - Newtonsoft.Json（JsonConvert.SerializeObject、DeserializeObject）
  - System.Text.Json（JsonSerializer）
  - JSON 配置文件读写实战

### 0.2 开发环境与项目结构
- Visual Studio 2022 工作区介绍
  - 解决方案资源管理器
  - 工具箱
  - 属性窗口
  - 错误列表与输出窗口
  - XAML 设计器与实时可视化树
- WPF 项目创建
  - WPF App (.NET) 项目模板
  - 项目文件结构详解
  - .csproj 项目文件说明
  - App.xaml / App.xaml.cs —— 应用程序入口
  - MainWindow.xaml / MainWindow.xaml.cs —— 主窗口
  - AssemblyInfo.cs 程序集信息
- 解决方案结构
  - 单项目 vs 多项目解决方案
  - 类库项目（Class Library）的作用
  - 项目引用与依赖管理
  - NuGet 包管理器使用
- 调试基础
  - 断点设置与条件断点
  - 单步执行（F10、F11）
  - 监视窗口与即时窗口
  - 调用堆栈查看
  - 输出调试信息（Debug.WriteLine）

---

## 1. WPF 快速入门

### 1.1 认识 WPF
- WPF 是什么？
  - 全称、历史、定位
  - 与 WinForms 的区别与优势
  - WPF 在工控/上位机领域的地位
- WPF 的特点
  - 矢量图形渲染
  - 声明式 UI（XAML）
  - 数据驱动 UI
  - 样式与模板分离
  - 分辨率无关（DPI 自适应）
  - 硬件加速（DirectX 底层）
- WPF 的应用场景
  - 工业上位机 / SCADA
  - 企业管理系统
  - 数据监控看板
  - 桌面工具软件
- WPF 的优势和劣势
  - 优势：灵活、强大、MVVM 友好、现代化
  - 劣势：学习曲线陡峭、运行时体积大
- WPF 的工作原理
  - 渲染管线（Visual Tree → Render Thread → GPU）
  - 保留模式 vs 即时模式
  - Dispatcher 线程模型
  - 布局-测量-排列两阶段流程

### 1.2 第一个 WPF 应用
- 创建 Hello World 项目
- 通过 XAML 添加控件
- 通过 C# 后台代码操控控件
- 理解 InitializeComponent() 方法
- 控件命名（x:Name）与后台引用
- 事件处理入门（Click 事件）

### 1.3 应用程序生命周期
- App.xaml 的 StartupUri
- Application 类详解
- 应用程序事件
  - Startup —— 启动时
  - Exit —— 退出时
  - Activated / Deactivated —— 激活/失活
  - DispatcherUnhandledException —— 全局异常捕获
  - SessionEnding —— 系统关机/注销
- Application.Current 全局访问
- ShutdownMode 属性（OnMainWindowClose、OnLastWindowClose、OnExplicitShutdown）
- 单实例应用实现

### 1.4 窗口 Window 详解
- Window 常用属性
  - Title —— 窗口标题
  - Width / Height —— 宽度/高度
  - MinWidth / MinHeight / MaxWidth / MaxHeight
  - WindowStartupLocation —— 启动位置（CenterScreen、CenterOwner、Manual）
  - WindowState —— 窗口状态（Normal、Minimized、Maximized）
  - WindowStyle —— 窗口样式（None、SingleBorderWindow、ThreeDBorderWindow、ToolWindow）
  - ResizeMode —— 调整大小模式
  - Topmost —— 置顶
  - ShowInTaskbar —— 任务栏显示
  - Icon —— 窗口图标
  - AllowsTransparency —— 允许透明（配合 WindowStyle=None 实现无边框窗口）
  - Background —— 背景
- Window 常用方法
  - Show() —— 非模态显示
  - ShowDialog() —— 模态显示
  - Close() —— 关闭
  - Hide() —— 隐藏
  - Activate() —— 激活
  - DragMove() —— 拖拽移动（无边框窗口）
- Window 常用事件
  - Loaded —— 加载完成
  - Closing —— 正在关闭（可取消）
  - Closed —— 已关闭
  - SizeChanged —— 大小改变
  - LocationChanged —— 位置改变
  - Activated / Deactivated
- 窗口传值与数据交互
  - 构造函数传参
  - 属性传值
  - 事件回调通知

---

## 2. XAML 详解

### 2.1 XAML 基础
- 什么是 XAML？
  - 全称：eXtensible Application Markup Language
  - XML 格式的声明式语言
  - 用于描述 UI 层次结构
  - XAML 编译到 BAML 的过程
- XAML 的特点
  - 声明式（"What you see" 而非 "How to do"）
  - UI 与逻辑分离
  - 强类型（编译时检查）
  - 支持命名空间扩展
- XAML 与 C# 代码的关系
  - XAML 能做 vs C# 能做的事
  - 哪些操作适合 XAML，哪些适合后台代码
- XAML 语法规则
  - 元素语法（Object Element Syntax）
  - 属性语法（Attribute Syntax）
  - 属性元素语法（Property Element Syntax）
  - 内容语法（Content Syntax）
  - 集合语法（Collection Syntax）
  - 事件特性语法（Event Attribute Syntax）

### 2.2 XAML 命名空间
- 默认命名空间
  - `xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"`
  - 映射 WPF 核心控件
- x: 命名空间
  - `xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"`
  - x:Key —— 资源键
  - x:Name —— 元素命名
  - x:Class —— 代码后置关联
  - x:Static —— 引用静态成员
  - x:Type —— 类型引用
  - x:Null —— 空值引用
  - x:Array —— 数组标记
- 自定义命名空间引入
  - `xmlns:local="clr-namespace:项目名"`
  - `xmlns:sys="clr-namespace:System;assembly=mscorlib"`

### 2.3 XAML 类派生关系
- WPF 核心类层次结构图
  - Object → DispatcherObject → DependencyObject → Visual → UIElement → FrameworkElement → Control
  - ContentControl 分支
  - ItemsControl 分支
  - Panel 分支
  - Shape 分支
- 理解 FrameworkElement 的核心能力
- 为什么要了解类继承关系

### 2.4 XAML 核心文件解读
- App.xaml 详解
  - StartupUri 作用
  - Application.Resources 全局资源定义
  - 合并资源字典（MergedDictionaries）
- MainWindow.xaml 详解
  - Window 根元素
  - xmlns 命名空间声明
  - x:Class 后台代码关联
  - 各种控件在 XAML 中的写法

---

## 3. 布局系统

### 3.1 布局基本概念
- WPF 布局特点
  - 流式布局（相对于 WinForms 绝对定位）
  - 两阶段布局（Measure + Arrange）
  - 分辨率自适应
- 常用布局属性（FrameworkElement）
  - Width / Height —— 固定宽高
  - MinWidth / MinHeight / MaxWidth / MaxHeight —— 最小/最大约束
  - Margin —— 外边距
  - Padding —— 内边距
  - HorizontalAlignment —— 水平对齐（Left、Center、Right、Stretch）
  - VerticalAlignment —— 垂直对齐（Top、Center、Bottom、Stretch）
  - Visibility —— 可见性（Visible、Hidden、Collapsed 的区别）
  - FlowDirection —— 流向
  - ActualWidth / ActualHeight —— 实际渲染尺寸（只读）
- 尺寸单位说明
  - 设备无关像素（DIP）
  - Auto 自动大小
  - * 星号比例（Grid专用）
  - NAN（double.NaN 表示自动）

### 3.2 Grid（网格布局）
- 核心特点：表格划分、灵活对齐
- 核心属性
  - RowDefinitions / ColumnDefinitions
  - RowDefinition 的 Height 模式（固定值、Auto、* 星号比例）
  - ColumnDefinition 的 Width 模式
  - Grid.Row / Grid.Column 附加属性
  - Grid.RowSpan / Grid.ColumnSpan 跨行跨列
  - ShowGridLines 调试辅助
- 尺寸模式详解
  - `Height="100"` —— 固定像素
  - `Height="Auto"` —— 根据内容自适应
  - `Height="*"` —— 按比例分配剩余空间
  - `Height="2*"` —— 多个星号的比例关系
- GridSplitter —— 可拖拽分隔条
- 用法示例与最佳实践
  - 经典三栏布局
  - 等分网格
  - 表单对齐布局

### 3.3 StackPanel（堆叠布局）
- 核心特点：垂直或水平线性排列
- 核心属性
  - Orientation —— 排列方向（Vertical / Horizontal）
- 用法示例
  - 导航菜单排列
  - 工具栏按钮排列
  - 表单字段排列
- StackPanel vs Grid 选择指南

### 3.4 WrapPanel（环绕布局）
- 核心特点：自动换行/换列排列
- 核心属性
  - Orientation —— 排列方向
  - ItemWidth / ItemHeight —— 统一子项尺寸
- 用法示例
  - 设备状态图标墙
  - 标签云
  - 图片缩略图排列

### 3.5 DockPanel（停靠布局）
- 核心特点：上下左右停靠
- 核心属性
  - LastChildFill —— 最后一个子元素是否填满剩余空间
  - DockPanel.Dock 附加属性（Top、Bottom、Left、Right）
- 用法示例
  - 经典软件主界面（顶部菜单→左侧导航→底部状态栏→中间内容区）

### 3.6 Canvas（画布布局）
- 核心特点：绝对定位/精确定位
- 核心属性
  - Canvas.Left / Canvas.Top 附加属性
  - Canvas.Right / Canvas.Bottom
  - ZIndex 控制叠加顺序（Panel.ZIndex）
- 用法示例
  - 可视化流程图节点定位
  - 自定义绘图
  - 拖拽定位
- 注意事项（分辨率适配差）

### 3.7 UniformGrid（均匀网格布局）
- 核心特点：等大小均匀划分
- 核心属性
  - Rows —— 行数
  - Columns —— 列数
  - FirstColumn —— 首个留空列
- 用法示例
  - 仪表盘数字键盘
  - 均匀按钮阵列

### 3.8 Border（边框容器）
- 核心特点：单个子元素 + 边框/背景
- 核心属性
  - BorderBrush —— 边框颜色
  - BorderThickness —— 边框粗细
  - CornerRadius —— 圆角半径
  - Background —— 背景填充
  - Padding —— 内容内边距
- 用法示例
  - 卡片式布局
  - 分组框
  - 高亮选中状态

### 3.9 ScrollViewer（滚动容器）
- 核心特点：内容溢出时提供滚动条
- 核心属性
  - HorizontalScrollBarVisibility —— 水平滚动条可见性
  - VerticalScrollBarVisibility —— 垂直滚动条可见性
  - CanContentScroll —— 是否按项目滚动
  - PanningMode —— 触控平移模式
- 用法示例
  - 长列表内容区域
  - 日志显示区
  - 内容可缩放区域

### 3.10 Viewbox（缩放容器）
- 核心特点：内容自动缩放适应容器
- 核心属性
  - Stretch —— 缩放模式（Uniform、Fill、UniformToFill、None）
  - StretchDirection —— 缩放方向
  - Child —— 单一子元素
- 用法示例
  - 仪表刻度自适应
  - SVG 图标缩放

### 3.11 布局容器选择策略
- 表单布局 → Grid + StackPanel 组合
- 主界面框架 → DockPanel + Grid
- 卡片/仪表盘 → WrapPanel + UniformGrid
- 流程图画布 → Canvas
- 所有内容需滚动 → ScrollViewer 包裹
- 复杂布局 → 嵌套使用（切忌过度嵌套）
- 布局性能优化建议

---

## 4. 控件体系

### 4.1 控件内容模型
- ContentControl（内容控件）
  - Content 属性（可容纳任意单个对象）
  - 典型子类：Button、Label、Window、ToolTip、UserControl、GroupBox
- HeaderedContentControl（带标题的内容控件）
  - Header 属性 + Content 属性
  - 典型子类：GroupBox、TabItem、Expander
- ItemsControl（条目控件）
  - Items / ItemsSource 属性
  - 典型子类：ListBox、ComboBox、ListView、TreeView、DataGrid、Menu、TabControl
- HeaderedItemsControl（带标题的条目控件）
  - Header 属性 + Items 属性
  - 典型子类：MenuItem、TreeViewItem、ToolBar
- 理解 Content 属性的强大之处（可放控件、布局、文本）

### 4.2 按钮类控件
- Button
  - Content（文本/图片/控件）
  - Click 事件
  - IsDefault / IsCancel（回车/ESC 快捷键）
  - IsEnabled
  - Command 与 CommandParameter（MVVM 绑定）
- RepeatButton（长按重复触发）
- ToggleButton（切换按钮）
  - IsChecked 属性
  - Checked / Unchecked / Indeterminate 事件
- RadioButton（单选按钮）
  - GroupName 分组
  - IsChecked 选中状态
- CheckBox（复选框）
  - IsChecked（true/false/null 三态）
  - IsThreeState 启用三态
  - Content 文本内容
- Image + Button 实现图标按钮

### 4.3 文本类控件
- Label
  - Target 属性（与 AccessText 配对实现快捷键标注）
  - Content 文本/控件
- TextBlock（轻量级文本显示）
  - Text 属性
  - TextWrapping（文本换行）
  - TextTrimming（文本截断）
  - Inlines（Run、Bold、Italic、Underline、Hyperlink 内联元素）
  - LineBreak 换行
  - FontFamily / FontSize / FontWeight / FontStyle
  - Foreground / Background
- TextBox（文本输入框）
  - Text 属性
  - TextWrapping 多行/单行
  - AcceptsReturn（允许回车换行）
  - MaxLength（最大字数限制）
  - IsReadOnly（只读）
  - SelectionStart / SelectionLength / SelectedText（选中操作）
  - TextChanged 事件
  - SpellCheck.IsEnabled（拼写检查）
  - VerticalScrollBarVisibility
- PasswordBox（密码输入框）
  - Password 属性（非明文存储）
  - PasswordChar（密码掩码字符）
  - MaxLength
  - PasswordChanged 事件
  - SecurePassword 属性（SecureString）
- RichTextBox（富文本编辑）
  - Document 属性（FlowDocument）
  - 文本格式设置
- AccessText（带助记符的文本）
  - 下划线前缀识别快捷键

### 4.4 选择类控件
- ComboBox（下拉选择框）
  - Items / ItemsSource 数据源
  - SelectedItem / SelectedValue / SelectedIndex
  - DisplayMemberPath
  - IsEditable（可编辑模式）
  - IsDropDownOpen
  - SelectionChanged 事件
  - DropDownOpened / DropDownClosed 事件
- ListBox（列表框）
  - ItemsSource 数据绑定
  - SelectedItem / SelectedItems（多选模式）
  - SelectionMode（Single、Multiple、Extended）
  - DisplayMemberPath
  - ItemTemplate 数据模板
  - VirtualizingStackPanel.IsVirtualizing（大数据量优化）
- ListView（列表视图）
  - View 属性（GridView 最常用）
  - GridViewColumn 列定义
  - DisplayMemberBinding 列绑定
  - CellTemplate 列模板
  - Header 列头
  - 排序、筛选
  - ItemContainerStyle（行样式）
- ComboBox vs ListBox vs ListView 选择指南

### 4.5 范围类控件
- Slider（滑块）
  - Minimum / Maximum —— 范围
  - Value —— 当前值
  - SmallChange / LargeChange —— 步进值
  - TickFrequency / TickPlacement —— 刻度
  - IsMoveToPointEnabled —— 点击直接跳转
  - Orientation —— 方向
  - IsSnapToTickEnabled —— 吸附刻度
- ProgressBar（进度条）
  - Minimum / Maximum
  - Value
  - IsIndeterminate —— 不确定模式（忙等动画）
  - Orientation
- ScrollBar（滚动条）

### 4.6 日期与信息显示控件
- DatePicker（日期选择器）
  - SelectedDate
  - DisplayDate
  - BlackoutDates（禁用日期集合）
  - SelectedDateFormat
  - SelectedDateChanged 事件
- Calendar（日历控件）
- Image（图片显示）
  - Source 属性
  - Stretch（拉伸方式）
  - StretchDirection
  - 使用相对路径、绝对路径、资源路径
- MediaElement（媒体播放器）
  - Source 路径
  - LoadedBehavior / UnloadedBehavior
  - Play() / Pause() / Stop() 方法
  - Position 播放进度
  - Volume / Balance
  - MediaOpened / MediaEnded 事件
- Popup（弹出层）
  - IsOpen
  - Placement / PlacementTarget
  - StaysOpen
  - AllowsTransparency
- ToolTip（工具提示）
  - ToolTip 属性（FrameworkElement / FrameworkContentElement 通用）
  - ToolTipService.ShowDuration / InitialShowDelay
  - 自定义 ToolTip 样式

### 4.7 容器与分组控件
- TabControl（选项卡/标签页）
  - Items / ItemsSource
  - SelectedIndex / SelectedItem
  - TabStripPlacement（标签方向）
  - ItemTemplate 选项卡头部模板
  - ContentTemplate 内容模板
- GroupBox（分组框）
  - Header 标题
  - 经典设置面板分组
- Expander（折叠面板）
  - Header 标题
  - IsExpanded 展开/折叠
  - ExpandDirection 展开方向
  - Collapsed / Expanded 事件
- Frame（页面框架，导航用）
- ContentPresenter / ItemsPresenter（模板用占位符）

### 4.8 菜单与工具栏控件
- Menu（菜单栏）
  - MenuItem 菜单项
  - Header 文本
  - Icon 图标
  - InputGestureText 快捷键提示
  - Command 绑定命令
  - Click 事件
  - IsCheckable / IsChecked
  - Separator 分隔线
- ContextMenu（右键菜单）
  - 通过 FrameworkElement.ContextMenu 附加
  - ContextMenuOpening 事件（动态构建菜单）
- ToolBar（工具栏）
  - ToolBarTray 多工具栏容器
  - Band / BandIndex 工具栏排列
  - OverflowMode 溢出模式
- StatusBar（状态栏）
  - StatusBarItem 子项
  - 常见布局：左侧消息 + 右侧进度/时间

### 4.9 装饰与辅助控件
- Separator（分隔线）
- GridSplitter（网格分割条）
- InkCanvas（手写/绘图画布）
- WebBrowser（内嵌浏览器控件）

### 4.10 对话框与交互
- MessageBox（消息弹窗）
  - Show() 方法重载
  - MessageBoxButton —— 按钮组合（OK、OKCancel、YesNo、YesNoCancel）
  - MessageBoxImage —— 图标（Information、Warning、Error、Question）
  - MessageBoxResult —— 返回值判断
  - 自定义 MessageBox 需求
- OpenFileDialog（打开文件对话框）
  - Filter 文件类型筛选
  - Multiselect 多选
  - InitialDirectory 初始目录
  - FileName / FileNames / SafeFileName
  - ShowDialog() 返回值判断
- SaveFileDialog（保存文件对话框）
  - Filter / InitialDirectory / FileName
  - DefaultExt 默认扩展名
- FolderBrowserDialog / OpenFolderDialog（选择文件夹）
  - 注意：WPF 原生无 FolderBrowserDialog，需引用 WinForms 或使用 FolderBrowser 第三方

### 4.11 用户控件与自定义控件
- UserControl（用户控件）
  - 创建步骤：添加 → 用户控件
  - XAML + 后置代码
  - 依赖属性暴露接口
  - 使用场景：页面子模块、可复用组件块
- CustomControl（自定义控件）
  - 无外观控件（Lookless Control）
  - Themes/Generic.xaml 默认样式
  - 继承 Control 类
  - 重写 OnApplyTemplate()
  - 使用场景：全新交互方式的控件、需主题切换的控件
- UserControl vs CustomControl 选择指南
  - UserControl：简单组装现有控件 + 固定外观 → UserControl
  - CustomControl：需要模板/主题支持 + 全新交互逻辑 → CustomControl

---

## 5. WPF 核心概念

### 5.1 依赖属性（Dependency Property）
- 什么是依赖属性？
  - 普通 CLR 属性的局限性
  - 依赖属性的核心作用：数据绑定、样式、动画、继承、默认值、变更通知
- 依赖属性的原理
  - DependencyObject 基类
  - 属性值计算优先级（11 层优先级）
  - 属性系统内部机制
- 注册依赖属性
  - DependencyProperty.Register()
  - 属性元数据（PropertyMetadata）
  - FrameworkPropertyMetadata
  - DefaultValue（默认值）
  - PropertyChangedCallback（变更回调）
  - CoerceValueCallback（强制值回调）
  - ValidateValueCallback（验证值回调）
  - AffectsRender、AffectsMeasure、AffectsArrange、AffectsParentArrange 标志
- 附加属性（Attached Property）
  - 什么是附加属性？（Grid.Row、Canvas.Left）
  - DependencyProperty.RegisterAttached()
  - GetXxx / SetXxx 静态方法
  - 自定义附加属性的实用场景（焦点附加、拖拽辅助）
- 依赖属性 vs CLR 属性包装
  - GetValue / SetValue
  - 不应当在包装器中放业务逻辑
- 依赖属性应用场景
  - 数据绑定源
  - 样式 Setter
  - 动画目标
  - 属性值继承（如 DataContext）
  - 元数据重写 OverrideMetadata

### 5.2 路由事件（Routed Event）
- 什么是路由事件？
  - 与 CLR 事件的区别
  - 路由事件的三大路由策略
- 路由策略
  - 冒泡（Bubbling）—— 从触发元素向上传递到根元素
  - 隧道（Tunneling）—— 从根元素向下传递到触发元素（Preview 前缀）
  - 直接（Direct）—— 仅触发元素自己
- 路由事件参数
  - RoutedEventArgs
  - Source —— 事件原始源
  - OriginalSource —— 事件原始源（考虑视觉树）
  - Handled —— 标记已处理（阻止继续路由）
  - RoutedEvent —— 路由事件标识
- 注册自定义路由事件
  - EventManager.RegisterRoutedEvent()
  - AddHandler / RemoveHandler
  - RaiseEvent 触发
- 附加事件（Attached Event）
  - 如 Button.Click 的非按钮元素使用
- 常用内置路由事件
  - MouseLeftButtonDown / PreviewMouseLeftButtonDown
  - MouseMove / PreviewMouseMove
  - KeyDown / PreviewKeyDown
  - GotFocus / LostFocus
  - Loaded / Unloaded
  - SizeChanged
- 隧道事件 vs 冒泡事件的执行顺序
  - 先隧道（Preview）→ 后冒泡（非Preview）
- 实战技巧
  - 全局鼠标事件监听
  - 键盘快捷键统一处理
  - e.Handled = true 阻止事件传播

### 5.3 命令系统（Command）
- 什么是命令？
  - 使用命令替代 Click 事件处理的好处
  - 命令的四大要素
- ICommand 接口
  - Execute(object parameter) —— 执行命令
  - CanExecute(object parameter) —— 是否可执行
  - CanExecuteChanged 事件
- 内置命令
  - ApplicationCommands（New、Open、Save、Close、Cut、Copy、Paste、Undo、Redo、Print、Help）
  - NavigationCommands
  - EditingCommands
  - ComponentCommands
  - MediaCommands
- 命令绑定
  - CommandBinding 绑定
  - Executed 事件处理
  - CanExecute 事件处理
  - CommandManager 命令管理器
- 自定义命令
  - RoutedUICommand 创建
  - InputGestureCollection 快捷键绑定
  - KeyGesture、MouseGesture
- MVVM 中的命令
  - RelayCommand / DelegateCommand 实现
  - 异步命令（AsyncRelayCommand）
  - 带参数的命令
- CommandParameter 参数传递
  - 简单值绑定
  - ElementName 引用其他控件
  - 相对路径查找 RelativeSource

### 5.4 数据绑定（Data Binding）
- 什么是数据绑定？
  - 数据源 → 绑定 → 目标
  - 单向 vs 双向
  - Binding 的工作流程
- Binding 核心属性
  - Path —— 绑定路径
  - Source —— 数据源对象
  - ElementName —— 绑到另一控件
  - RelativeSource —— 相对源（Self、TemplatedParent、FindAncestor、PreviousData）
  - Mode —— 绑定模式
    - OneWay（源→目标）
    - TwoWay（双向）
    - OneTime（仅初始化，上控件常用）
    - OneWayToSource（反向绑定）
    - Default（各控件默认不同）
  - UpdateSourceTrigger —— 更新源时机
    - PropertyChanged（实时更新）
    - LostFocus（失焦更新）
    - Explicit（手动调用更新）
  - Converter —— 值转换器（IValueConverter）
  - ConverterParameter —— 转换器参数
  - StringFormat —— 格式化字符串
  - FallbackValue —— 绑定失败时的默认值
  - TargetNullValue —— 源为 null 时的默认值
  - Delay —— 延迟更新（减少频繁更新）
  - NotifyOnValidationError —— 验证错误通知
  - ValidatesOnDataErrors —— 数据错误验证
- DataContext（数据上下文）
  - 什么是 DataContext？
  - 属性值继承机制（重要！）
  - Window.DataContext → 子控件自动继承
  - 在 XAML 中设置 vs 在代码中设置
  - DataContext 查找逻辑
- INotifyPropertyChanged 接口
  - 为什么需要 INotifyPropertyChanged？
  - PropertyChanged 事件实现
  - CallerMemberName 简化写法
  - ObservableObject 基类封装
  - 集合变更通知：INotifyCollectionChanged
- 绑定表达式高级用法
  - 多级属性路径（Company.Name）
  - 索引器路径（Items[0]）
  - 空路径绑定（{Binding}）
  - 多绑定 MultiBinding + IMultiValueConverter
  - 优先绑定 PriorityBinding
- RelativeSource 详解
  - `{Binding RelativeSource={RelativeSource Self}}` —— 绑自己
  - `{Binding RelativeSource={RelativeSource TemplatedParent}}` —— 模板父级
  - `{Binding RelativeSource={RelativeSource AncestorType={x:Type Window}}}` —— 找上级
- ElementName 绑定
  - `{Binding ElementName=slider1, Path=Value}` —— 绑另一控件
- 值转换器 IValueConverter
  - 实现 Convert / ConvertBack
  - 常用场景：bool→Visibility、值→颜色、状态→文字
  - 多值转换器 IMultiValueConverter
- 数据验证
  - ValidationRules 验证规则
  - ExceptionValidationRule
  - 自定义 ValidationRule
  - IDataErrorInfo / INotifyDataErrorInfo
  - 验证错误模板（Validation.ErrorTemplate）
- 调试数据绑定
  - PresentationTraceSources.TraceLevel
  - 输出窗口查看绑定错误
  - Snoop 工具查看实际绑定值

### 5.5 标记扩展（Markup Extension）
- 什么是标记扩展？
- 语法格式：`{ExtensionName Parameter}`
- 常用内置标记扩展
  - `{Binding}` —— 数据绑定
  - `{StaticResource key}` —— 静态资源引用
  - `{DynamicResource key}` —— 动态资源引用
  - `{x:Static}` —— 引用静态成员
  - `{x:Type}` —— 类型引用
  - `{x:Null}` —— null 值
  - `{x:Array}` —— 数组定义
  - `{RelativeSource}` —— 相对源
  - `{TemplateBinding}` —— 模板绑定（控件模板中使用）
  - `{FindAncestor}` —— 查找父级元素
- StaticResource vs DynamicResource
  - 加载时机差异
  - 性能差异
  - 适用场景选择
  - 常见误区
- 自定义标记扩展
  - 继承 MarkupExtension
  - 重写 ProvideValue 方法
  - 应用场景：简单值生成、服务定位

### 5.6 资源系统（Resource）
- 什么是 WPF 资源？
  - 资源 = 可复用的对象（画笔、样式、模板、字符串等）
- 资源层级与查找顺序
  - 控件级 → 父控件级 → Window级 → Application级 → 系统级
  - 就近原则（StaticResource）
- 资源定义与引用
  - FrameworkElement.Resources
  - Application.Resources
  - x:Key 键名
  - {StaticResource} / {DynamicResource}
- 资源字典（ResourceDictionary）
  - 独立资源文件 .xaml
  - MergedDictionaries 合并
  - 代码中加载资源字典
  - 按模块组织资源文件
- 二进制资源
  - BuildAction 属性：Resource / Content / Embedded Resource
  - 图片资源的 Pack URI
  - `pack://application:,,,/Resources/xxx.png`
- 字符串资源与国际化基础

### 5.7 样式（Style）
- 什么是样式？
  - 一组属性设置的集合
  - 可复用、可继承
  - 解耦外观与逻辑
- Style 核心属性
  - TargetType —— 目标控件类型
  - x:Key —— 样式键名（具名样式）
  - BasedOn —— 样式继承
  - Setters —— 属性设置集合
  - Triggers —— 触发器集合
  - Resources —— 样式级资源
- Setter 详解
  - Property + Value 模式
  - 事件 Setter（EventSetter）
- 样式继承（BasedOn）
  - 基础样式 → 派生样式
  - 属性覆盖
- 隐式样式（不设置 x:Key）
  - 自动应用到所有 TargetType 控件
  - 作用范围控制
- 样式的作用域与优先级
- 上位机中样式使用技巧
  - 颜色规范统一定义
  - 状态色（运行绿、停止红、报警黄）
  - 字体大小层级定义

### 5.8 模板（Template）
- 控件模板（ControlTemplate）
  - 改变控件视觉结构，保留行为逻辑
  - 无外观控件（Lookless）理念
  - ControlTemplate 定义
  - TemplateBinding 继承控件属性
  - ContentPresenter 占位符
  - Triggers 模板触发器
  - VisualStateManager（VSM）视觉状态管理
  - 自定义控件的 Templates/Generic.xaml
- 数据模板（DataTemplate）
  - 数据驱动的UI展示
  - 不同类型数据用不同模板
  - DataTemplate 定义与使用
  - 隐式数据模板（DataType 自动匹配）
  - DataTemplateSelector 模板选择器
- HierarchicalDataTemplate（层级数据模板）
  - TreeView + 递归模板
  - ItemsSource 层级绑定
- ItemContainerStyle（条目容器样式）
  - ListBoxItem / ListViewItem 样式
  - AlternationCount 交替行颜色
- 模板绑定语法
  - `{TemplateBinding PropertyName}` —— 单向
  - `{Binding RelativeSource={RelativeSource TemplatedParent}}` —— 支持双向
  - `{Binding RelativeSource={RelativeSource AncestorType=...}}` —— 在DataTemplate中找父级

### 5.9 触发器（Trigger）
- 属性触发器（Trigger）
  - 基于属性值触发样式变更
  - EnterActions / ExitActions
- 多条件触发器（MultiTrigger）
  - Conditions 集合
  - 所有条件满足才触发
- 数据触发器（DataTrigger）
  - 基于绑定数据值触发
  - Value 匹配
- 多数据触发器（MultiDataTrigger）
- 事件触发器（EventTrigger）
  - RoutedEvent 触发
  - BeginStoryboard 启动动画
  - SoundPlayerAction
- 各触发器适用场景对比
  - Trigger → 控件自己的属性
  - DataTrigger → ViewModel 中的数据
  - EventTrigger → 动画
  - MultiTrigger → 多条件组合

### 5.10 各核心概念之间的联系（关系图）
- 依赖属性 ← 数据绑定/样式/动画的基础
- 依赖属性 + 数据绑定 → MVVM 的桥梁
- 路由事件 + 命令 → 用户交互的两种方式
- 资源系统 → 样式/模板/画笔的存储容器
- 样式 → 复用属性设置
- 模板 → 改变的视觉树，依赖样式资源
- 触发器 → 样式的动态行为
- 数据模板 → 数据驱动显示的核心

---

## 6. 图形与多媒体

### 6.1 WPF 图形渲染概述
- 保留模式 vs 即时模式
- 矢量图形 vs 位图
- 分辨率无关性原理
- 视觉树（Visual Tree）与逻辑树（Logical Tree）

### 6.2 Shape 基本图形
- Line（直线）
  - X1、Y1、X2、Y2
  - Stroke、StrokeThickness、StrokeDashArray
- Rectangle（矩形）
  - Width、Height
  - RadiusX、RadiusY（圆角）
  - Fill、Stroke
- Ellipse（椭圆/圆形）
  - Width = Height 时为正圆
  - Fill、Stroke
- Polygon（多边形）
  - Points 点集合
  - FillRule（填充规则）
- Polyline（折线/不闭合多边形）
  - Points 点集合
- Path（路径，功能最强大）
  - Data 属性（路径标记语法）
  - 几何图形组合
- 所有 Shape 共享属性
  - Fill —— 填充画刷
  - Stroke —— 描边画刷
  - StrokeThickness —— 描边粗细
  - StrokeDashArray —— 虚线样式
  - StrokeStartLineCap / StrokeEndLineCap —— 线端样式
  - Stretch —— 拉伸模式
  - RenderTransform —— 渲染变换
  - LayoutTransform —— 布局变换

### 6.3 Path 与 Geometry
- Geometry 类型
  - LineGeometry —— 线段
  - RectangleGeometry —— 矩形
  - EllipseGeometry —— 椭圆
  - PathGeometry —— 复杂路径（Figures、Segments）
  - StreamGeometry —— 轻量路径（性能更优，不可绑定）
  - CombinedGeometry —— 组合几何
  - GeometryGroup —— 几何组
- 路径标记语法（Path Markup Syntax / 微语言）
  - M（Move）—— 移动起点
  - L（Line）—— 画线
  - H（Horizontal Line）—— 水平线
  - V（Vertical Line）—— 垂直线
  - C（Cubic Bezier）—— 三次贝塞尔曲线
  - Q（Quadratic Bezier）—— 二次贝塞尔曲线
  - A（Arc）—— 弧线
  - Z（Close）—— 闭合路径
- 上位机应用场景
  - 自定义仪表盘绘制
  - 实时曲线组件
  - 自定义 LED 指示灯
  - 状态图标

### 6.4 Brush 画刷
- SolidColorBrush（纯色画刷）
  - Color 颜色（RGB/ARGB/命名颜色）
  - Colors 静态颜色类
  - Opacity 不透明度
- LinearGradientBrush（线性渐变）
  - StartPoint / EndPoint
  - GradientStops 渐变停止点
  - Offset / Color
- RadialGradientBrush（径向渐变）
  - Center / GradientOrigin
  - RadiusX / RadiusY
  - GradientStops
- ImageBrush（图像画刷）
  - ImageSource 图像源
  - Stretch 拉伸
  - Viewbox / Viewport
- VisualBrush（可视画刷）
  - Visual 属性（复制任意UI元素的视觉效果）
  - 场景：放大镜效果、缩略图
- DrawingBrush（绘图画刷）
  - Drawing 内容
- 上位机画刷应用
  - 设备状态渐变色（绿→黄→红）
  - 表盘渐变背景

### 6.5 Transform 变换
- RotateTransform（旋转）
  - Angle（角度）
  - CenterX / CenterY（旋转中心）
- ScaleTransform（缩放）
  - ScaleX / ScaleY
  - CenterX / CenterY
- SkewTransform（倾斜/错切）
  - AngleX / AngleY
- TranslateTransform（平移）
  - X / Y
- TransformGroup（变换组合）
- MatrixTransform（矩阵变换，底层/高级）
- RenderTransform vs LayoutTransform
  - 区别与选择

### 6.6 Effect 特效
- BlurEffect（模糊）
  - Radius 模糊半径
- DropShadowEffect（投影）
  - ShadowDepth、BlurRadius、Color、Direction、Opacity
  - 为上控件卡片增加层次感
- 性能注意事项
  - Effect 触发软件渲染，影响性能
  - 适可而止

### 6.7 图像处理
- Image 控件
  - Source 属性设置
  - 资源路径（Pack URI）
  - Stretch 属性
- BitmapImage
  - UriSource
  - DecodePixelWidth / DecodePixelHeight
  - CacheOption
  - CreateOptions
- WriteableBitmap（可写入位图）
  - 像素级操作
  - 绘制热力图、自定义图形
  - 高性能实时更新
- RenderTargetBitmap（渲染到位图）
  - 将 UIElement 渲染为位图
  - 截图功能实现

### 6.8 音频与视频
- MediaElement 媒体播放
  - Source 媒体源路径
  - LoadedBehavior / UnloadedBehavior（Manual 手动控制）
  - Play()、Pause()、Stop()
  - Position（播放位置）
  - Volume / Balance / SpeedRatio
  - MediaOpened / MediaEnded / MediaFailed 事件
- SoundPlayer（System.Media）
  - 播放系统声音
  - 播放 WAV 文件
- 上位机场景
  - 报警声音提示
  - 操作确认音效
  - 操作演示视频

### 6.9 2D/3D 图形基础
- 2D 绘图综合
  - DrawingVisual 轻量绘图
  - OnRender 重写
  - 自定义高性能图形
- 3D 图形入门
  - Viewport3D 视口
  - PerspectiveCamera / OrthographicCamera
  - ModelVisual3D
  - 光照（AmbientLight、DirectionalLight、PointLight、SpotLight）
  - Material（DiffuseMaterial、SpecularMaterial、EmissiveMaterial）
  - MeshGeometry3D（网格几何）
  - 3D 模型变换
  - 上位机应用：3D 产线模型展示

### 6.10 动画（Animation）
- 动画基础
  - 什么是 WPF 动画？
  - 动画类型：属性动画 vs 路径动画
  - 时间线（Timeline）概念
  - Duration、AutoReverse、RepeatBehavior、BeginTime、SpeedRatio
- 基础动画类型
  - DoubleAnimation —— 浮点属性动画
  - ColorAnimation —— 颜色属性动画
  - PointAnimation —— 点属性动画
  - StringAnimationUsingKeyFrames —— 字符串关键帧动画
- 关键帧动画
  - LinearDoubleKeyFrame（线性）
  - DiscreteDoubleKeyFrame（离散跳变）
  - SplineDoubleKeyFrame（样条曲线缓动）
  - EasingDoubleKeyFrame（缓动函数）
- 缓动函数（Easing Functions）
  - BackEase、BounceEase、CircleEase、CubicEase、ElasticEase
  - ExponentialEase、PowerEase、QuadraticEase、SineEase
  - 为上位机添加灵动感
- Storyboard 故事板
  - 组织多个动画
  - BeginStoryboard 触发
  - Pause、Resume、Stop、Seek 控制
  - FillBehavior 属性
  - Completed 事件
- 动画在上位机的应用
  - 数值平滑过渡（仪表指针移动）
  - 报警闪烁
  - 状态切换过渡
  - 页面切换动画
  - 加载等待动画

---

## 7. MVVM 设计模式

### 7.1 MVVM 基础概念
- 什么是 MVVM？
  - Model - View - ViewModel 三层
  - 职责分离思想
- MVVM vs MVC vs MVP 对比
- 为什么要用 MVVM？
  - 分离关注点
  - 可测试性
  - 可维护性
  - 团队并行开发
- MVVM 各层职责
  - Model：数据模型、业务逻辑、数据访问
  - View：纯 XAML UI 展示、零业务代码
  - ViewModel：连接 View 和 Model、处理UI逻辑、状态管理

### 7.2 Model 层
- 数据实体定义
- 数据验证逻辑
- 数据访问（Repository模式）
- DTO（数据传输对象）vs Entity
- 枚举类型定义

### 7.3 View 层
- 纯 XAML + 最小化后台代码
- DataContext 绑定到 ViewModel
- 控件的 Command 绑定
- 尽量不使用 Click 事件
- View 的职责边界

### 7.4 ViewModel 层
- 继承 INotifyPropertyChanged
- 属性变更通知实现
  - 传统手动通知
  - CallerMemberName 简化
  - ObservableObject 基类
  - Fody.PropertyChanged 自动织入
- ICommand 实现
  - RelayCommand 无参数
  - RelayCommand\<T\> 带参数
  - AsyncRelayCommand 异步命令
- ViewModel 间的通信
  - 父子 ViewModel 关系
  - 事件聚合器 / 消息中心
- ViewModel 的生命周期
  - 初始化（构造函数/初始化方法）
  - 加载数据
  - 释放资源

### 7.5 主流 MVVM 框架
- CommunityToolkit.Mvvm（推荐首选）
  - 微软官方维护
  - 源生成器（Source Generator）减少样板代码
  - [ObservableProperty] 属性标记
  - [RelayCommand] 命令标记
  - ObservableObject、ObservableRecipient
  - WeakReferenceMessenger 消息机制
  - Ioc.Default 简单依赖注入
- Prism
  - 企业级框架
  - 区域（Region）管理
  - 模块化（Module）
  - 导航（Navigation）
  - 事件聚合器（EventAggregator）
  - 依赖注入容器
  - 适用场景：大型复杂项目
- MVVM Light Toolkit
  - 经典轻量级框架（已停止维护，了解即可）
- ReactiveUI
  - 响应式编程范式
  - 数据流处理

### 7.6 依赖注入（DI/IoC）
- 什么是依赖注入？
  - 控制反转（IoC）
  - 依赖倒置原则（DIP）
- 容器组成
  - 服务注册（Register）
  - 服务解析（Resolve）
  - 生命周期管理（Transient、Singleton、Scoped）
- 常用 DI 容器
  - Microsoft.Extensions.DependencyInjection（官方推荐）
  - 服务注册
  - ViewModel 注入到 View
  - 服务接口与实现分离
- DI 在 MVVM 中的应用
  - ViewModel 通过构造函数注入服务
  - View 通过容器解析 ViewModel

### 7.7 MVVM 实战：从零搭建
- 项目结构
  ```
  Project/
  ├── Models/          数据模型
  ├── ViewModels/      视图模型
  ├── Views/           XAML 视图
  ├── Services/        业务服务
  ├── Helpers/         辅助工具
  ├── Converters/      值转换器
  └── Resources/       样式资源
  ```
- 导航服务实现
  - 页面切换
  - 导航参数传递
  - 导航历史管理
- 对话框服务
  - IDialogService 接口
  - MessageBox 封装
  - 自定义弹窗
- 配置服务
  - appsettings.json
  - IOptions\<T\> 绑定
  - 配置热更新
- 日志服务
  - ILogger 接口
  - Serilog / NLog 集成

---

## 8. 线程与异步

### 8.1 WPF 线程模型
- 主线程（UI Thread / Dispatcher Thread）
- 后台线程
- STA（Single Thread Apartment）单线程单元
- Dispatcher 消息循环
- 为什么不能在非 UI 线程访问控件

### 8.2 Dispatcher 调度器
- Dispatcher.Invoke —— 同步调用（阻塞等待）
- Dispatcher.BeginInvoke —— 异步调用（不阻塞）
- DispatcherPriority 优先级
  - Normal、Background、Input、Render、Loaded、Send 等
- DispatcherTimer
  - Interval 间隔
  - Tick 事件（在UI线程执行）
  - IsEnabled
  - 适用场景：UI 定时刷新
- 检查是否需要调度
  - Dispatcher.CheckAccess()
  - Application.Current.Dispatcher

### 8.3 Task 与 async/await
- async / await 详解
  - 标记异步方法
  - 不阻塞 UI 线程
  - ConfigureAwait(false) 的使用
  - 异步方法的异常捕获
- Task.Run —— 将工作交给线程池
- Task.Delay —— 异步延迟（不阻塞UI）
- 从UI线程安全更新控件
  - Progress\<T\> + IProgress\<T\> 模式
  - SynchronizationContext
- 取消异步操作
  - CancellationTokenSource
  - CancellationToken
  - ThrowIfCancellationRequested
- 并行任务
  - Task.WhenAll —— 等待所有完成
  - Task.WhenAny —— 任意一个完成即返回
  - Parallel.For / Parallel.ForEach

### 8.4 BackgroundWorker（经典/了解）
- DoWork 事件（后台线程）
- ProgressChanged 事件（UI 线程）
- RunWorkerCompleted 事件（UI 线程）
- 与 async/await 的对比
- 现代替代方案

### 8.5 多线程同步与安全
- lock 语句
- Monitor
- Mutex（跨进程互斥）
- Semaphore / SemaphoreSlim
- ReaderWriterLockSlim
- ConcurrentQueue\<T\> / ConcurrentDictionary\<TKey, TValue\>
- Interlocked 原子操作
- 死锁的成因与避免

### 8.6 上位机多线程设计模式
- 生产者-消费者模式
  - BlockingCollection\<T\>
  - 数据采集线程 → 缓冲区 → UI线程
- 定时数据采集模式
  - System.Timers.Timer（后台线程触发）
  - DispatcherTimer（UI线程触发）
  - PeriodicTimer（.NET 6+）
- 线程池 vs 专用线程选择

---

## 9. 上位机通信协议

### 9.1 通信协议概述
- 上位机通信的应用场景
  - PLC 连接
  - 传感器读取
  - 仪器仪表通信
  - 机器人控制
- 通信模型分类
  - 串行通信 vs 并行通信
  - 同步通信 vs 异步通信
  - 单工、半双工、全双工
- OSI 七层模型简化理解
- 上位机通信协议全景图

### 9.2 串口通信（SerialPort）
- 串口通信基础概念
  - RS-232 / RS-485 / RS-422 区别
  - 波特率（Baud Rate）：9600、19200、38400、57600、115200
  - 数据位（Data Bits）：7、8
  - 停止位（Stop Bits）：1、1.5、2
  - 校验位（Parity）：None、Odd、Even、Mark、Space
  - 流控制（Handshake）
- System.IO.Ports.SerialPort 类
  - PortName —— 端口名（COM1、COM2...）
  - BaudRate —— 波特率
  - DataBits —— 数据位
  - StopBits —— 停止位
  - Parity —— 校验
  - IsOpen —— 是否打开
  - Open() / Close()
  - Read() / ReadLine() / ReadExisting() / ReadByte()
  - Write() / WriteLine()
  - BytesToRead —— 可读字节数
  - ReadTimeout / WriteTimeout
  - DtrEnable / RtsEnable
- 串口事件
  - DataReceived 事件（数据到达，后台线程！）
  - ErrorReceived 事件
  - PinChanged 事件
- 串口通信调试
  - 虚拟串口工具（VSPD）
  - 串口调试助手
  - 常见问题排查
  - 断线重连机制
- 串口数据接收最佳实践
  - 粘包/分包处理
  - 数据帧缓存
  - 超时处理
  - 校验和 / CRC 校验
- 上位机串口实战示例
  - 完整的串口数据收发类封装
  - 多串口并发管理

### 9.3 Socket 网络通信
- 网络基础概念
  - IP 地址与端口
  - TCP vs UDP 区别
  - 服务端（Server）/客户端（Client）模型
  - Socket 工作流程
- TCP 通信
  - TcpListener（服务端）
    - 初始化端口监听
    - AcceptTcpClient / AcceptSocket
    - 多客户端并发处理
  - TcpClient（客户端）
    - Connect 连接
    - GetStream() 获取网络流
    - NetworkStream 读写
  - TCP 特点：可靠、有序、有连接
- UDP 通信
  - UdpClient
    - 发送：Send()
    - 接收：Receive()
  - UDP 特点：无连接、不可靠、速度快
  - 广播与组播
- 异步通信
  - async / await 异步收发
  - 高并发处理策略
  - 断线重连与心跳机制
  - 环形缓冲区管理
- Socket 通信实战
  - 自定义通信协议（帧头+数据+校验+帧尾）
  - 大小端序转换（BitConverter、BinaryPrimitives）
  - 字节数组与结构体转换

### 9.4 Modbus 通信协议
- Modbus 协议概述
  - Modbus 历史与现状
  - 主站（Master）/从站（Slave）架构
- Modbus RTU（基于串口）
  - 帧格式
  - 功能码（01~06、15、16）
  - CRC16 校验
  - 线圈（Coil）、离散输入、保持寄存器、输入寄存器
- Modbus TCP（基于网口）
  - MBAP 报文头
  - 帧格式
  - 默认端口 502
- 常用功能码
  - 01 —— 读线圈
  - 02 —— 读离散输入
  - 03 —— 读保持寄存器
  - 04 —— 读输入寄存器
  - 05 —— 写单个线圈
  - 06 —— 写单个寄存器
  - 15 —— 写多个线圈
  - 16 —— 写多个寄存器
- 常用库
  - NModbus4
  - EasyModbus
  - FluentModbus
- Modbus 上位机实战
  - PLC 连接与读写
  - 寄存器地址映射
  - 批量读取优化
  - 异常处理

### 9.5 OPC 协议
- OPC 概述
  - OPC Classic（DA、HDA、A&E）
  - OPC UA（统一架构）
  - OPC 在工业自动化的地位
- OPC UA 核心概念
  - 地址空间（Address Space）
  - 节点（Node）与引用（Reference）
  - 数据访问、历史访问、报警与事件
  - 安全模型（加密、认证）
- OPC UA .NET 开发
  - OPC Foundation UA .NET Standard
  - 客户端开发
    - 连接服务器
    - 浏览节点
    - 读取/写入值
    - 订阅（Subscription）数据变化
  - 服务器开发简介
- 与 PLC 的 OPC UA 连接

### 9.6 MQTT 协议
- MQTT 概述
  - 发布/订阅模型
  - Broker（MQTT 服务器）
  - 轻量级、低带宽、IoT 首选
- MQTT 核心概念
  - Topic（主题）与通配符
  - QoS（服务质量等级 0、1、2）
  - Retained Message（保留消息）
  - Last Will（遗嘱消息）
  - Keep Alive（心跳保活）
- .NET 中使用 MQTT
  - MQTTnet 库
  - 连接 Broker（如 EMQX、Mosquitto）
  - 发布消息
  - 订阅主题
  - 消息处理
- 上位机 MQTT 应用
  - 设备状态上报
  - 远程控制指令下发
  - 多终端数据同步

### 9.7 其他通信方式
- USB / HID 通信
  - USB HID 设备读取
  - LibUsbDotNet 库
  - 适用场景：扫码枪、RFID读卡器
- CAN 总线
  - CAN 协议概述
  - PCAN / Kvaser 等硬件适配
  - SocketCAN
- WebSocket
  - 全双工通信
  - 适用场景：Web端实时看板
  - ClientWebSocket 类
- HTTP API 调用
  - HttpClient
  - GET / POST 请求
  - RESTful API 对接 MES/ERP 系统
  - 请求重试与超时

### 9.8 通信协议选择策略
- 近距离一对一 → RS-232 / RS-485
- PLC 设备 → Modbus RTU / TCP
- 工厂级集成 → OPC UA
- IoT / 远程监控 → MQTT
- 局域网数据传输 → TCP / UDP
- Web 端集成 → HTTP / WebSocket
- 通信方式选型决策树

---

## 10. 数据采集与处理

### 10.1 数据采集系统架构
- 采集系统总体设计
  - 数据采集层 → 数据处理层 → 数据存储层 → 数据展示层
- 采集策略
  - 轮询（Polling）：定时查询
  - 事件驱动（Event-Driven）：数据到达触发
  - 订阅模式（Subscription）：OPC/MQTT 订阅
- 采集线程模型设计
  - 各通道独立线程
  - 统一数据汇聚

### 10.2 实时数据处理
- 数据解析
  - 字节序转换（大端/小端）
  - 数据类型转换（byte[]→int/float/double/string）
  - BitConverter / BinaryPrimitives
  - 位运算提取标志位
  - 自定义协议帧解析
- 数据校验
  - 校验和（Checksum）
  - CRC8 / CRC16 / CRC32
  - LRC（纵向冗余校验）
  - 错误帧处理策略
- 数据过滤与清洗
  - 野值/异常值剔除
  - 滑动平均滤波
  - 中值滤波
  - 低通滤波
  - 死区过滤
- 数据转换与工程值计算
  - 原始值 → 工程值（量程转换）
  - 线性/非线性标定
  - 公式计算引擎

### 10.3 数据存储
- 本地文件存储
  - JSON 格式（配置/少量数据）
  - XML 格式
  - CSV 格式（数据导出）
  - 二进制格式（高性能大容量）
- 轻量级数据库 SQLite
  - NuGet 引用 System.Data.SQLite / Microsoft.Data.Sqlite
  - 创建数据库与表
  - CRUD 操作
  - 参数化查询防注入
  - Dapper 轻量 ORM
  - Entity Framework Core + SQLite
- 关系型数据库
  - SQL Server（工控常见）
  - MySQL / PostgreSQL
  - Entity Framework Core
  - 数据库连接字符串
  - 连接池管理
- 时序数据库
  - 什么是时序数据库？
  - InfluxDB、TimescaleDB、TDengine 简介
  - 为何上位机需考虑时序数据库
- 存储策略
  - 实时数据 → 内存缓存
  - 历史数据 → 数据库/文件
  - 数据保留策略（定期清理）
  - 数据备份

### 10.4 数据可视化
- 实时曲线/趋势图
  - LiveCharts2（推荐）
    - 安装与配置
    - 折线图（LineSeries）
    - 柱状图（ColumnSeries）
    - 饼图（PieSeries）
    - 实时更新策略
    - 滚动视图与缩放
    - 多 Y 轴
  - OxyPlot
    - PlotModel / PlotView
    - LineSeries / ScatterSeries
    - 实时数据追加与移除
    - 交互操作（缩放、平移、跟踪）
    - 导出图片
- 仪表盘控件
  - 自绘表盘（Path + Arc）
  - 圆形/弧形进度条
  - 第三方仪表控件
  - 表盘动画（数值平滑过渡）
- 状态指示灯
  - 圆形灯控件（运行/停止/报警）
  - 闪烁动画
  - 颜色自定义
- 报警系统
  - 报警条件配置
  - 报警等级（提示/警告/严重）
  - 报警列表显示（DataGrid）
  - 报警确认与消音
  - 报警记录存储
  - 声音/弹窗/闪烁提醒

### 10.5 数据导入导出
- 导出 Excel
  - EPPlus / ClosedXML
  - 带格式导出（表头、颜色、单元格合并）
  - 大数据量分批导出
- 导出 PDF
  - iTextSharp / PdfSharp
- 导出 CSV
- 报表生成
  - 日报、月报、年报
  - 报表模板
  - 定时自动生成

---

## 11. WPF 高级 UI 开发

### 11.1 自定义控件深度开发
- 控件生命周期
  - 构造函数 → InitializeComponent → OnInitialized → OnApplyTemplate → Loaded → OnRender
- 自定义依赖属性
  - Register / RegisterAttached
  - 属性变更回调
  - 强制值回调
  - 元数据选项
- 自定义路由事件
  - EventManager.RegisterRoutedEvent
  - AddHandler / RemoveHandler
  - RaiseEvent
- 控件重绘
  - OnRender(DrawingContext dc)
  - DrawingContext 绘图 API
  - 高性能自绘控件
- 焦点管理
  - Focusable / IsTabStop / TabIndex
  - Focus() / Keyboard.Focus()
  - GotFocus / LostFocus
  - 焦点Scope

### 11.2 数据模板高级应用
- DataTemplateSelector
  - 根据数据类型/属性选择不同模板
  - 重写 SelectTemplate 方法
- DataTemplate 中的事件绑定
  - 使用 RelativeSource AncestorType
- HierarchicalDataTemplate 层级数据
  - TreeView 数据绑定完整示例
- ItemContainerStyle
  - 列表项交替颜色
  - 选中状态样式

### 11.3 主题与换肤
- 资源字典组织主题
- 动态切换主题（Light/Dark 模式）
- 主题色系统
- MaterialDesignInXAML 主题定制
- HandyControl 主题
- 自定义主题文件结构

### 11.4 多语言与国际化
- 资源文件（.resx）
- ResourceManager 动态读取
- WPF Localization Extension
- 语言切换
- 上位机中英文切换方案

### 11.5 拖拽功能（Drag & Drop）
- 控件间拖拽
  - AllowDrop = true
  - DragEnter / DragOver / DragLeave / Drop 事件
  - DragDrop.DoDragDrop()
- 文件拖入
  - e.Data.GetData(DataFormats.FileDrop)
  - 拖入文件路径获取
- 自定义拖拽数据格式

### 11.6 WPF 与 Windows API 交互
- P/Invoke 基础
  - DllImport 特性
  - 常用 Win32 API 封装
- 常用 Win32 API
  - SetWindowPos —— 窗口置顶/调整
  - GetForegroundWindow —— 获取前台窗口
  - SendMessage / PostMessage —— 窗口消息
  - GetSystemMetrics —— 获取系统信息
  - RegisterHotKey / UnregisterHotKey —— 全局快捷键
- Windows API Code Pack

### 11.7 第三方 UI 控件库
- MaterialDesignInXAML
  - 安装配置
  - 基础控件样式
  - 颜色主题
  - 对话框
  - 图标包
- HandyControl
  - 丰富的工控风格控件
  - Gauge 仪表
  - StepBar 步骤条
  - Tag 标签
  - 消息提示 Growl
  - 侧边导航
- ModernWPF（Windows 10/11 风格）
- LiveCharts2（图表）
- Extended WPF Toolkit
  - ColorPicker
  - DateTimePicker
  - PropertyGrid
  - BusyIndicator

### 11.8 响应式布局与自适应
- 百分比布局策略
- Viewbox 缩放适配
- 监听 SizeChanged 动态调整
- 不同分辨率适配
- 多屏适配（拼接屏场景）

### 11.9 高 DPI 适配
- DPI 感知模式设置
- app.manifest 配置
- Per-Monitor DPI Awareness
- 图片资源多分辨率版本

### 11.10 触控与手势
- Manipulation 事件
  - ManipulationStarting / ManipulationDelta / ManipulationCompleted
- 触控旋转、缩放、平移
- 适应工业触摸屏场景

---

## 12. 系统架构与设计模式

### 12.1 上位机软件架构概述
- 架构设计的重要性
- 常见上位机架构类型
- 架构设计原则（SOLID）

### 12.2 分层架构
- 三层架构
  - 表示层（Presentation Layer / UI）
  - 业务逻辑层（Business Logic Layer / BLL）
  - 数据访问层（Data Access Layer / DAL）
- 各层职责与交互
- 层间通信规则
- 依赖方向（高层→低层）

### 12.3 插件化架构
- 什么是插件化架构？
- 使用 MEF（Managed Extensibility Framework）
- 使用 Prism Modules
- 动态加载 DLL
- 上位机插件化场景
  - 不同设备驱动作为插件
  - 不同通信协议作为插件
  - 功能模块热插拔

### 12.4 常用设计模式
- 单例模式（Singleton）
  - 线程安全单例
  - 上位机应用：全局配置管理、日志管理器
- 工厂模式（Factory）
  - 简单工厂 / 工厂方法 / 抽象工厂
  - 上位机应用：通信驱动工厂、设备驱动工厂
- 观察者模式（Observer） / 发布-订阅
  - 事件机制
  - 上位机应用：数据变化通知、报警广播
- 策略模式（Strategy）
  - 上位机应用：不同协议解析策略、不同报警策略
- 适配器模式（Adapter）
  - 上位机应用：统一不同设备API
- 外观模式（Facade）
  - 上位机应用：通信层统一入口
- 模板方法模式（Template Method）
  - 上位机应用：数据采集流程框架
- 状态模式（State）
  - 上位机应用：设备状态管理

### 12.5 配置文件管理
- appsettings.json（推荐）
  - Microsoft.Extensions.Configuration
  - ConfigurationBuilder
  - 绑定到强类型对象
  - IOptions\<T\>
- App.config / Web.config 传统方式
- XML / JSON / INI 自定义配置
- 配置加密
- 运行时可修改配置

### 12.6 日志系统
- 为什么要用日志？
- 日志级别（Debug、Info、Warn、Error、Fatal）
- 常用日志框架
  - Serilog（推荐）
    - 结构化日志
    - 输出到文件、控制台、数据库、Seq
    - 日志模板
    - Enricher 丰富器
  - NLog
  - Log4Net
- 全局异常捕获与记录
  - AppDomain.CurrentDomain.UnhandledException
  - Application.Current.DispatcherUnhandledException
  - TaskScheduler.UnobservedTaskException
  - UI 友好错误提示
- 上位机日志场景
  - 通信记录
  - 操作日志
  - 报警日志
  - 系统错误

### 12.7 软件更新方案
- ClickOnce 部署与更新
- 自动更新检测与下载
- 增量更新（差分更新）
- 回滚机制
- 版本号管理

### 12.8 测试
- 单元测试
  - xUnit / NUnit / MSTest
  - 测试 ViewModel 逻辑
  - Moq / NSubstitute 模拟依赖
  - 测试覆盖率
- UI 自动化测试
  - FlaUI / Appium
  - UI 测试策略
- 集成测试
- 工控软件测试要点

---

## 13. 性能优化与调试

### 13.1 WPF 渲染原理深入
- 视觉树（Visual Tree）
- 渲染线程（Render Thread）与 UI 线程
- 保留模式渲染
- Dirty Rect 更新区域
- 硬件加速条件
- Tier 0 / Tier 1 / Tier 2 渲染层级

### 13.2 UI 性能优化
- UI 虚拟化（Virtualization）
  - VirtualizingStackPanel
  - VirtualizingPanel.IsVirtualizing
  - VirtualizingPanel.VirtualizationMode（Standard / Recycling）
  - ScrollUnit（Pixel / Item）
  - 容器回收机制
- 延迟加载
  - 数据分页
  - 虚拟化集合
  - 懒加载控件
- 减少视觉树复杂度
  - 避免过度嵌套
  - 合理使用布局容器
  - 隐藏而非销毁（Visibility.Collapsed）
- 冻结（Freeze）Freezable 对象
  - 画刷冻结
  - 画笔冻结
  - BitmapImage 缓存
- 异步绑定
  - IsAsync=True
  - Binding Delay
- 图片优化
  - 解码尺寸控制
  - 缓存策略
- 位图缓存（BitmapCache）
  - CacheMode
  - RenderOptions.BitmapScalingMode
- 避免频繁布局计算
  - SuspendLayout 概念替代
  - 批量更新
- 减少动画消耗
  - 动画帧率控制
  - 硬件加速动画

### 13.3 内存管理
- WPF 内存常见问题
  - 内存泄漏
  - 大对象堆碎片
- 常见泄漏场景
  - 事件未取消订阅（最频繁）
  - 弱事件模式（WeakEventManager）
  - 静态引用
  - 定时器未停止
  - BitmapImage 未释放
  - 绑定导致的泄漏
- 资源释放
  - IDisposable 模式
  - using 语句
  - 析构函数
- 内存分析工具
  - Visual Studio 诊断工具
  - dotMemory
  - ANTS Memory Profiler
  - WinDbg + SOS

### 13.4 性能分析工具
- Visual Studio 诊断工具
  - CPU 使用率
  - 内存使用率
  - 时间线
- WPF Performance Suite（Perforator、Visual Profiler）
- Snoop / WPF Inspector
  - 查看实时视觉树
  - 查看数据上下文
  - 修改属性测试
- 性能计数器（Performance Counters）

### 13.5 常见性能陷阱
- 大量控件同时可见
- 频繁的 UI 更新（应节流/防抖）
- 未使用数据虚拟化的大列表
- 过度使用 DropShadowEffect / BlurEffect
- 布局循环（Layout Cycle）
- 大量数据绑定不必要通知
- 字符串频繁拼接（用 StringFormat / StringBuilder）

### 13.6 调试技巧
- XAML 调试
  - XAML 热重载
  - 实时可视化树
  - XAML 绑定失败排查
- 数据绑定调试
  - PresentationTraceSources.TraceLevel=High
  - 输出窗口查看警告
  - Snoop 绑定调试
- 运行时调试
  - 条件断点
  - 跟踪点（Tracepoint）
  - 并行堆栈窗口（多线程调试）
  - 即时窗口执行

---

## 14. 工业级实战项目

### 14.1 项目一：温湿度监控系统（入门级）
- 项目概述
  - 功能：实时采集温湿度、曲线显示、超限报警、数据记录
  - 技术点：串口通信、MVVM基础、LiveCharts曲线、数据存储
- 需求分析
- 系统设计（架构图 + 界面原型）
- 通信协议设计
- 串口数据采集模块实现
- ViewModel 设计
- 实时曲线与仪表盘界面
- 报警功能实现
- 历史数据存储与查询
- 项目总结

### 14.2 项目二：Modbus PLC 数据采集看板（进阶级）
- 项目概述
  - 功能：连接 PLC、读取多寄存器、实时看板显示、参数下发
  - 技术点：Modbus 协议、多设备管理、看板布局、读写分离
- 需求分析
- Modbus 通信层封装
- 设备管理模块
- 采集调度策略
- 看板 UI 设计
- 参数读写功能
- 项目总结

### 14.3 项目三：产线设备状态监控平台（进阶级）
- 项目概述
  - 功能：多产线多设备状态监控、OEE 计算、状态日志
  - 技术点：多线程采集、状态机、数据可视化
- 系统架构设计
- 设备状态机设计
- 通信管理（多串口/多Socket）
- OEE 指标计算
- 大屏可视化看板
- 数据库存储
- 项目总结

### 14.4 项目四：智能仓储管理系统 WMS（中高级）
- 项目概述
  - 功能：库存管理、出入库、库位可视化、RFID扫描
  - 技术点：数据库设计、MVVM进阶、Prism模块化
- 需求与数据库设计
- Prism 模块划分
- 库存管理功能
- RFID 通信集成
- 库位可视化展示
- 出入库流程
- 项目总结

### 14.5 项目五：工业视觉检测上位机（中高级）
- 项目概述
  - 功能：相机采集、图像显示、检测结果显示、参数配置
  - 技术点：相机SDK对接、图像处理、高速显示
- 相机通信（GigE Vision / USB3 Vision）
- 图像显示与 ROI 区域
- 检测逻辑集成
- 结果统计与展示
- 项目总结

### 14.6 项目六：多轴运动控制平台（高级）
- 项目概述
  - 功能：多轴电机控制、轨迹规划、示教回放
  - 技术点：运动控制卡API、多轴联动、实时曲线
- 运动控制卡通信
- 轴状态监控
- Jog 点动与定位
- 轨迹编程与回放
- 项目总结

### 14.7 项目七：SCADA 综合监控系统（高级/综合）
- 项目概述
  - 功能：多站点监控、组态画面、报警管理、报表系统
  - 技术点：OPC UA、插件化、组态化、大型架构
- SCADA 系统架构
- 组态化设计
- OPC UA 数据源对接
- 报警规则引擎
- 历史趋势与报表
- 权限管理与审计
- 项目总结

---

## 15. 部署与发布

### 15.1 发布方式
- ClickOnce 发布
- MSI 安装包（WiX Toolset）
- 独立发布（Self-Contained）
- 单文件发布（Single File）
- 框架依赖 vs 独立部署比较

### 15.2 部署注意事项
- 目标机器 .NET Runtime 检查
- 依赖项打包
- 配置文件处理
- 开机自启动设置
- Windows 服务模式

### 15.3 版本管理
- 语义版本号（SemVer）
- Assembly Version vs File Version
- 自动更新策略

---

## 16. 开源项目与学习资源

### 16.1 GitHub 优质 WPF 开源项目
- MaterialDesignInXAML —— Material Design 风格 UI 库
- HandyControl —— 国产工控风格 UI 库
- LiveCharts2 —— 交互式图表库
- OxyPlot —— 科学绘图库
- Prism —— MVVM 框架
- CommunityToolkit.Mvvm —— 微软 MVVM 工具包
- PropertyChanged.Fody —— 自动属性通知
- NModbus —— Modbus 协议库
- MQTTnet —— MQTT 客户端
- Serilog —— 结构化日志库
- Dapper —— 轻量 ORM
- FlaUI —— UI 自动化测试

### 16.2 上位机相关开源项目
- 各类开源的 PLC 通信库
- 开源 SCADA 项目
- 工控看板模板项目

### 16.3 推荐书籍
- 《WPF 编程宝典》（Pro WPF in C#）
- 《深入浅出 WPF》
- 《WPF 核心技术》
- 《C# 高级编程》
- 《C# 并发编程经典实例》
- 工控/自动化相关书籍

### 16.4 在线教程与文档
- Microsoft Docs WPF 官方文档
- Microsoft Learn WPF 学习路径
- YouTube WPF 教程推荐
- B站 WPF 教程推荐
- 博客园 WPF 精华文章

### 16.5 技术社区
- Stack Overflow（wpf标签）
- GitHub Discussions
- 博客园
- CSDN
- .NET 中文社区
- 知乎 WPF 话题

### 16.6 常用 NuGet 包清单
- 数据：Newtonsoft.Json、System.Text.Json、Dapper、Microsoft.EntityFrameworkCore
- UI：MaterialDesignThemes、HandyControl、LiveChartsCore、OxyPlot.Wpf
- MVVM：CommunityToolkit.Mvvm、Prism.Unity
- 通信：System.IO.Ports、NModbus4、MQTTnet、OPCFoundation.NetStandard.Opc.Ua
- 日志：Serilog、NLog
- Excel：EPPlus、ClosedXML
- 测试：xUnit、Moq、FlaUI

### 16.7 开发工具清单
- Visual Studio 2022 Community
- JetBrains ReSharper（编码辅助）
- Snoop（UI调试）
- WPF Performance Suite（性能分析）
- dotMemory（内存分析）
- VSPD（虚拟串口）
- MQTTX（MQTT 调试客户端）
- Modbus Poll / Modbus Slave（Modbus 调试）

### 16.8 学习路线图
```
第一阶段（1~2周）
├── C# 基础：变量、流程控制、类与对象、集合、LINQ
├── 开发环境搭建
└── 控制台小程序练习

第二阶段（2~3周）
├── WPF 项目结构
├── XAML 语法与命名空间
├── 布局系统（Grid → StackPanel → DockPanel → ...）
└── 基础控件（Button → TextBox → ComboBox → ListBox → ...）

第三阶段（3~4周）
├── 数据绑定（重点！）
├── 依赖属性
├── 路由事件与命令
├── 样式与模板
└── 用户控件

第四阶段（3~4周）
├── MVVM 模式
├── CommunityToolkit.Mvvm
├── 简单 CRUD 应用实战
└── 依赖注入

第五阶段（3~4周）
├── 串口通信
├── Socket 通信
├── Modbus 协议
└── 多线程与异步

第六阶段（4~6周）
├── 数据采集完整实战
├── 实时曲线与仪表盘
├── 数据存储
└── 报警系统

第七阶段（持续）
├── 架构设计优化
├── 性能优化
├── 工业级项目实战
└── 持续学习新技术
```
